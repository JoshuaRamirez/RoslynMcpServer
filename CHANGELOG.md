# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/), and this project adheres to [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Changed
- `move_type_to_namespace` now honors optional **`allFiles`**: omitted / false keeps today's single-site move (`sourceFile` required, `symbolName` required, `targetNamespace` required, including `line` / `column` disambiguation, `updateFileLocation` default false, preview, and MissingRequiredParam / InvalidSourcePath / SourceFileNotFound / InvalidNamespace / SameLocation / SymbolAmbiguous / NameCollision / nested exclusion throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), and for every top-level `TypeDeclarationSyntax` (class/struct/interface/record) whose current namespace is not already `targetNamespace` moves with today's `ValidateNamespaceChangeAsync` / `ComputeChangesAsync` / optional `PlanFileLocationUpdate` / commit helpers (same document filter as `FormatDocumentOperation` / `MoveTypeToFileOperation.ExecuteAllFilesAsync` / `RenameFileToMatchTypeOperation.ExecuteAllFilesAsync` / `UseBaseTypeOperation.ExecuteAllFilesAsync` / `EncapsulateFieldOperation.ExecuteAllFilesAsync`). Nested types, types already in `targetNamespace` (SameLocation), NameCollision, multi-type files (`ComputeChangesAsync` would reject), types without a namespace declaration, uneditable documents, and resolution failures are skipped rather than failing the walk. When `updateFileLocation` is true and two types would claim the same destination in one walk, the later claim is skipped (deterministic; same `DestinationsReferToSameLocation` semantics as `rename_file_to_match_type` / `move_type_to_file`). Optional `sourceFile` when `allFiles` is true limits the walk to that one file; omit it to walk the whole solution. Bulk walks every eligible type (not a broader search for one `symbolName`). If every type is a no-op, succeeds with empty changes (does not throw the single-site SourceFileNotFound / SymbolAmbiguous / NameCollision / SameLocation / InvalidNamespace error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `symbolName`, `line`, or `column` (those stay single-site). `targetNamespace` stays required. `updateFileLocation` / `preview` remain valid in bulk mode. `preview: true` with `allFiles: true` returns aggregated `PendingChanges` and writes nothing / moves nothing. This leftover does not invent a second tool, `rename_namespace` allFiles, or `convert_to_block_body` allFiles.
- `move_type_to_file` now honors optional **`allFiles`**: omitted / false keeps today's single-site move (`sourceFile` required, `symbolName` required, `targetFile` required, including `line` / `column` disambiguation, `createTargetFile` default true, preview, and MissingRequiredParam / InvalidSourcePath / InvalidTargetPath / SourceFileNotFound / SameLocation / SymbolAmbiguous / NameCollision / nested exclusion throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), and for every top-level `TypeDeclarationSyntax` (class/struct/interface/record) derives the destination as `{currentFileDirectory}/{TypeName}.cs` and moves with today's `ValidateTargetAsync` / `ComputeChangesAsync` / commit helpers (same document filter as `FormatDocumentOperation` / `RenameFileToMatchTypeOperation.ExecuteAllFilesAsync` / `UseBaseTypeOperation.ExecuteAllFilesAsync` / `EncapsulateFieldOperation.ExecuteAllFilesAsync`). Nested types, already well-placed single-type matching files (file stem equals type name and the file has exactly one top-level type — same spirit as `rename_file_to_match_type` allFiles no-op), SameLocation (including case-only paths on a case-insensitive volume, same `DestinationsReferToSameLocation` semantics as `rename_file_to_match_type`), NameCollision / occupied destinations today's `ValidateTargetAsync` would reject, destinations that exist on disk but are not in the workspace, uneditable documents, and resolution failures are skipped rather than failing the walk. When two types would claim the same destination in one walk, the later claim is skipped (deterministic). Moving the last eligible type keeps leftover enums, delegates, and global statements in the source file rather than deleting it. Optional `sourceFile` when `allFiles` is true limits the walk to that one file; omit it to walk the whole solution. Bulk walks every eligible type (not a broader search for one `symbolName`). If every type is a no-op, succeeds with empty changes (does not throw the single-site SourceFileNotFound / SymbolAmbiguous / NameCollision / SameLocation error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `symbolName`, `targetFile`, `line`, or `column` (those stay single-site). `createTargetFile` (default true) / `preview` remain valid in bulk mode; when `createTargetFile` is false and the derived destination is missing, that type is skipped. `preview: true` with `allFiles: true` returns aggregated `PendingChanges` and writes nothing / moves nothing. This leftover does not invent a second tool or `convert_to_block_body` allFiles.
- `use_base_type` now honors optional **`allFiles`**: omitted / false keeps today's single-site use (`sourceFile` required, `typeName` required, including `line` / `column` disambiguation, `targetBaseType`, preview, and TypeNotFound / InvalidSymbolKind / NoCommonBase / BaseClassNotFound / NoEligibleReferences / BaseCannotSatisfyUsedMembers / DocumentNotEditable throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), and for every `TypeDeclarationSyntax` (class/struct/interface) resolves the symbol, picks a target via today's `GetTargetBaseType` (optional `targetBaseType` filters: skip types that lack that base), collects eligible rewritable references via `FindTypeReferencesAsync` / `CanUseBaseTypeAsync` / `ApplyRewritesAsync`, and aggregates `FilesModified` / preview (same document filter as `FormatDocumentOperation` / `EncapsulateFieldOperation.ExecuteAllFilesAsync` / `AddNullChecksOperation.ExecuteAllFilesAsync` / `ConvertToAsyncOperation` / `ConvertPropertyOperation` / `InvertIfOperation`). Per-type `InvalidSymbolKind`, `NoCommonBase`, `BaseClassNotFound`, `NoEligibleReferences`, `BaseCannotSatisfyUsedMembers`, `DocumentNotEditable`, and similar failures are skipped rather than failing the walk. A type whose rewrites would collapse sibling overloads onto the same signature (`M(Dog)` + `M(Cat)` both becoming `M(Animal)`, CS0111) when combined with already-accepted rewrites is also skipped. Bulk walks every eligible type (not a broader search for one `typeName`). If every type is a no-op, succeeds with empty changes (does not throw the single-type TypeNotFound / NoEligibleReferences / NoCommonBase error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `typeName`, `line`, or `column` (those stay single-site). `targetBaseType` / `preview` remain valid in bulk mode. `preview: true` with `allFiles: true` returns aggregated `PendingChanges` and writes nothing. This leftover does not invent a second tool or `convert_to_block_body` allFiles.
- `encapsulate_field` now honors optional **`allFiles`**: omitted / false keeps today's single-site encapsulate (`sourceFile` required, `fieldName` required, including `line` / `column` disambiguation, `propertyName` derivation, `readOnly` default false, `updateReferences` default true, preview, and FieldNotFound / InvalidSelection / NameCollision throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), and encapsulates every eligible field `VariableDeclaratorSyntax` (same filter as `FindFieldDeclarator`; locals excluded) using today's `DerivePropertyName` / `SyntaxGenerationHelper.CreatePropertyFromField` helpers (same document filter as `FormatDocumentOperation` / `AddNullChecksOperation.ExecuteAllFilesAsync` / `ConvertToAsyncOperation` / `ConvertPropertyOperation` / `InvertIfOperation`). Const fields (`InvalidSelection`), fields whose derived property name already exists (`NameCollision`), and other per-field failures are skipped rather than failing the walk. `readOnly` / `updateReferences` / `preview` remain valid in bulk mode. If every file is a no-op, succeeds with empty changes (does not throw the single-file FieldNotFound / NameCollision / InvalidSelection error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `fieldName`, `line`, `column`, or `propertyName` (those stay single-site; bulk cannot apply one propertyName). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool or `convert_to_block_body` / `use_base_type` allFiles.
- `add_null_checks` now honors optional **`allFiles`**: omitted / false keeps today's single-site add (`sourceFile` required, `methodName` required, including `line` / `column` disambiguation, `style` default throw, preview, and MethodNotFound / NoMembersToGenerate / no-body throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), and inserts checks at the start of every method or constructor with a block body whose parameters still need them using the existing `NullCheckGenerator.ShouldCheckForNull` / `GenerateThrowIfNull` / `GenerateGuardClause` helpers (same document filter as `FormatDocumentOperation` / `ConvertToAsyncOperation.ExecuteAllFilesAsync` / `ConvertPropertyOperation` / `InvertIfOperation`). Methods with no parameters requiring checks (`NoMembersToGenerate`), no body, an expression body, or every eligible parameter already guarded (`ThrowIfNull` / if-throw) are skipped rather than failing the walk; already-checked parameters are not duplicated. `style` and `preview` remain valid in bulk mode. If every file is a no-op, succeeds with empty changes (does not throw the single-file MethodNotFound / NoMembersToGenerate error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `methodName`, `line`, or `column` (those stay single-site). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool or `convert_to_block_body` / `encapsulate_field` allFiles.
- `rename_file_to_match_type` now honors optional **`allFiles`**: omitted / false keeps today's single-file rename (`sourceFile` required, including `typeName` / `line` / `column` disambiguation, preview, and SameLocation / TargetFileExists / TypeNotFound / SymbolAmbiguous throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), and renames each file that declares exactly one top-level type whose name does not match the file stem using the existing `FindTopLevelTypes` / `MoveSourceFile` / optional project compile-item rewrite helpers (same document filter as `FormatDocumentOperation` / sibling AllFiles ops). Multi-type, zero-type, already-matching (`FileNameMatchesType`), destination-occupied (`IsDestinationOccupiedByDifferentFile`), and uneditable documents are skipped rather than failing the walk. A physical file linked into several projects is moved once; every owning document and project still receives workspace / compile-item updates. Two mismatched files that would claim the same destination (per `ReferToSameFile` filesystem semantics — distinct casings stay distinct on a case-sensitive volume) are both skipped. A mid-batch `MoveSourceFile` failure rolls back completed moves so a half-applied rename is never left on disk. Case-only renames keep today's `MoveSourceFile` temp-path behavior. If every file is a no-op, succeeds with empty changes (does not throw the single-file SameLocation / TypeNotFound / SymbolAmbiguous error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `typeName`, `line`, or `column` (those stay single-site). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (delete old path + create new path, plus project updates when applicable) and writes nothing / moves nothing. This leftover does not invent a second tool or `convert_to_block_body` allFiles.
- `convert_to_async` now honors optional **`allFiles`**: omitted / false keeps today's single-site convert (`sourceFile` required, `methodName` required, including `line` / `column` disambiguation, `renameToAsync` default true, `updateCallers` default false, preview, and already-async / iterator / not-found throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), converts every distinct eligible sync method per file using the existing convert helpers (same document filter as `FormatDocumentOperation` / `ConvertPropertyOperation.ExecuteAllFilesAsync` / `InvertIfOperation` / `ConvertToPatternMatchingOperation` / `ConvertForeachLinqOperation` / `ConvertToInterpolatedStringOperation` / `ConvertExpressionBodyOperation` / `SimplifyNameOperation` / `AddBracesOperation` / `RemoveBracesOperation`), commits once, and aggregates `FilesModified`. Already-async, iterator, unresolved, override, and otherwise unsupported methods (no awaitable calls) or unchanged documents are skipped rather than failing the walk. Project entry-point / traditional static `Main` is converted in place (not renamed to `MainAsync`) so the console entry point stays valid. Nested call-site rewrites compose so `A(B())` becomes `await AAsync(await BAsync())` when `updateCallers` is true. Method-group and `nameof` references are renamed with the converted method. Multiple methods convert once each in a single rewrite pass so the same node is never double-converted. Call-site identifier rewrites follow `renameToAsync` (default true). `updateCallers` (default false) uses the existing await-wrap rules (already-async callers only; skip sync callers that cannot legally await). If every file is a no-op, succeeds with empty changes (does not throw the single-file "not found" / already-async / iterator error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `methodName`, `line`, or `column` (those stay single-site). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool or `convert_to_block_body` allFiles.
- `convert_property` now honors optional **`allFiles`**: omitted / false keeps today's single-site convert (`sourceFile` required, plus `propertyName` or `line`, including `column` disambiguation, preview, and CannotConvert throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), converts every distinct eligible property in the requested `direction` per file using the existing convert helpers (same document filter as `FormatDocumentOperation` / `InvertIfOperation.ExecuteAllFilesAsync` / `ConvertToPatternMatchingOperation` / `ConvertForeachLinqOperation` / `ConvertToInterpolatedStringOperation` / `ConvertExpressionBodyOperation` / `SimplifyNameOperation` / `AddBracesOperation` / `RemoveBracesOperation`), commits once, and aggregates `FilesModified`. Missing-accessors, already-auto, already-full, interface, abstract, and otherwise ineligible properties or unchanged documents are skipped rather than failing the walk. Multiple properties in one type convert once each; `ToFullProperty` inserts a backing field per converted auto-property in a single rewrite pass so the same node is never double-converted and field insertion indices stay correct. The backing field copies `static` from the property and receives any auto-property initializer. If every file is a no-op, succeeds with empty changes (does not throw the single-file "not found" / CannotConvert error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `propertyName`, `line`, or `column` (those stay single-site). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. Direction (`ToAutoProperty` / `ToFullProperty`) is unchanged. This leftover does not invent a second tool or `convert_to_block_body` / `convert_to_async` allFiles.
- `invert_if` now honors optional **`allFiles`**: omitted / false keeps today's single-site invert (`sourceFile` required, `line` required, including `column` disambiguation, preview, and incomplete-if throw). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), inverts every distinct eligible if per file using the existing invert helpers (same document filter as `FormatDocumentOperation` / `ConvertToPatternMatchingOperation.ExecuteAllFilesAsync` / `ConvertForeachLinqOperation` / `ConvertToInterpolatedStringOperation` / `ConvertExpressionBodyOperation` / `SimplifyNameOperation` / `AddBracesOperation` / `RemoveBracesOperation`), commits once, and aggregates `FilesModified`. Incomplete or otherwise ineligible ifs and unchanged documents are skipped rather than failing the walk. Nested ifs invert once each in a single rewrite pass (innermost first) so the same node is never double-inverted. If every file is a no-op, succeeds with empty changes (does not throw the single-file "not found" / ConditionNotInvertible error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `line` or `column` (those stay single-site). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool or `convert_to_block_body` / `convert_property` / `convert_to_async` allFiles.
- `convert_to_pattern_matching` now honors optional **`allFiles`**: omitted / false keeps today's single-site convert (`sourceFile` required, `line` required, including `column` disambiguation and preview). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), converts every distinct eligible switch or if/is-chain per file using the existing conversion helpers (same document filter as `FormatDocumentOperation` / `ConvertForeachLinqOperation.ExecuteAllFilesAsync` / `ConvertToInterpolatedStringOperation` / `ConvertExpressionBodyOperation` / `SimplifyNameOperation` / `AddBracesOperation` / `RemoveBracesOperation`), commits once, and aggregates `FilesModified`. Statements that cannot convert and unchanged documents are skipped rather than failing the walk. If every file is a no-op, succeeds with empty changes (does not throw the single-file "not found" / CannotConvert error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `line` or `column` (those stay single-site). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool or `convert_to_block_body` / `convert_property` / `invert_if` / `convert_to_async` allFiles.
- `convert_foreach_linq` now honors optional **`allFiles`**: omitted / false keeps today's single-foreach convert (`sourceFile` required, `line` required, including `column` disambiguation, `preferQuerySyntax`, and preview). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), converts every distinct eligible foreach per file using the existing conversion helpers (same document filter as `FormatDocumentOperation` / `ConvertToInterpolatedStringOperation.ExecuteAllFilesAsync` / `ConvertExpressionBodyOperation` / `SimplifyNameOperation` / `AddBracesOperation` / `RemoveBracesOperation`), applies the same `preferQuerySyntax` setting to each, commits once, and aggregates `FilesModified`. Foreach that cannot convert and unchanged documents are skipped rather than failing the walk. If every file is a no-op, succeeds with empty changes (does not throw the single-file "not found" error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `line` or `column` (those stay single-foreach). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool or new LINQ patterns.
- `convert_to_interpolated_string` now honors optional **`allFiles`**: omitted / false keeps today's single-expression convert (`sourceFile` required, `line` required, including `column` disambiguation and preview). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), converts every distinct convertible `string.Format` invocation and outer concatenation per file (same document filter as `FormatDocumentOperation` / `ConvertExpressionBodyOperation.ExecuteAllFilesAsync` / `SimplifyNameOperation` / `AddBracesOperation` / `RemoveBracesOperation`), commits once, and aggregates `FilesModified`. Expressions that cannot convert and unchanged documents are skipped rather than failing the walk. If every file is a no-op, succeeds with empty changes (does not throw the single-file "not found" error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `line` or `column` (those stay single-expression). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. This leftover does not invent a second tool.
- `convert_expression_body` now honors optional **`allFiles`**: omitted / false keeps today's single-member convert (`sourceFile` required, plus `memberName` or `line`, including `column` disambiguation and preview). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), converts every eligible supported member in the requested `direction` per file (same document filter as `FormatDocumentOperation` / `SimplifyNameOperation.ExecuteAllFilesAsync` / `AddBracesOperation` / `RemoveBracesOperation`), commits once, and aggregates `FilesModified`. Already-in-target-form and otherwise ineligible members or documents are skipped rather than failing the walk. If every file is a no-op, succeeds with empty changes (does not throw a whole-walk "cannot convert" error). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `memberName`, `line`, or `column` (those stay single-member). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing. Direction (`ToExpressionBody` / `ToBlockBody`) is unchanged. This leftover does not invent a second tool.
- `remove_braces` now honors optional **`allFiles`**: omitted / false keeps today's single-file remove braces (`sourceFile` required, including statement/file/type scopes and preview). Default `scope` remains `statement` for single-file use. `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), applies file-scope brace removal per file (same document filter as `FormatDocumentOperation` / `SimplifyNameOperation.ExecuteAllFilesAsync` / `AddBracesOperation`), commits once, and aggregates `FilesModified`. Files with nothing to unwrap are skipped. If every file is a no-op, succeeds with empty changes (does not throw a whole-walk "nothing to unwrap" error). `sourceFile` is optional when `allFiles` is true. Omitted `scope` with `allFiles: true` uses file-scope (CLI `--all-files` without `--scope` succeeds). Explicit `allFiles: true` cannot be combined with `scope=statement` or `scope=type` (those stay single-file). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing.
- `add_braces` now honors optional **`allFiles`**: omitted / false keeps today's single-file add braces (`sourceFile` required, including statement/file/type scopes and preview). Default `scope` remains `statement` for single-file use. `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), applies file-scope brace wrapping per file (same document filter as `FormatDocumentOperation` / `SimplifyNameOperation.ExecuteAllFilesAsync`), commits once, and aggregates `FilesModified`. Files with nothing to wrap are skipped. If every file is a no-op, succeeds with empty changes (does not throw a whole-walk "nothing to wrap" error). `sourceFile` is optional when `allFiles` is true. Omitted `scope` with `allFiles: true` uses file-scope (CLI `--all-files` without `--scope` succeeds). Explicit `allFiles: true` cannot be combined with `scope=statement` or `scope=type` (those stay single-file). `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing.
- `simplify_name` now honors optional **`allFiles`**: omitted / false keeps today's single-file simplify (`sourceFile` required, including file-scope `NoSimplifiableNames` and location-scope). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), applies file-scope simplify per file, commits once, and aggregates `FilesModified`. Files with nothing to simplify are skipped. If every file is a no-op, succeeds with empty changes (does not throw `NoSimplifiableNames`). `sourceFile` is optional when `allFiles` is true. `allFiles: true` cannot be combined with `scope=location`. `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing.
- `format_document` now honors optional **`allFiles`**: omitted / false keeps today's single-file format (`sourceFile` required, including already-formatted no-op and preview). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), formats each with `Formatter.FormatAsync`, commits once, and aggregates `FilesModified`. Already-formatted files are skipped. `sourceFile` is optional when `allFiles` is true. `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing.
- `sort_usings` now honors optional **`allFiles`**: omitted / false keeps today's single-file sort (`sourceFile` required, including `systemFirst` grouping and already-sorted no-op). `allFiles: true` walks every C# document in the solution (`FilePath` ends with `.cs`), sorts usings per file with the same `UsingDirectiveSorter.Sort(..., systemFirst)` rules, commits once, and aggregates `FilesModified`. Already-sorted files are skipped. `sourceFile` is optional when `allFiles` is true. `preview: true` with `allFiles: true` returns aggregated `PendingChanges` (one per file that would change) and writes nothing.
- `inline_constant` now honors optional **`line`** / **`column`**: omitted keeps today's constantName + optional typeName path (`FieldDeclarationSyntax` name match; `SymbolAmbiguous` → provide typeName when several constants share the name). Do not invent a new omitted-line FirstOrDefault across types. `TypeName` stays an additive filter; line/column do not replace it. Set `line` picks the matching const field declarator whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest covering declarator/field — same exclusive-end coverage as `encapsulate_field` / `SpanCoversLine`). Nested types participate. When `line` is set, a field whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. Set `column` with `line` picks the matching const field whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest covering declarator/field — same exclusive-end coverage as `encapsulate_field` / `SpanCoversColumn`). `column` without `line` cannot disambiguate same-indent same-name constants across lines (today's omitted-line path after the name/typeName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line field does not also match the previous declaration. If `line` is set (with or without `column`) and nothing covers that position, `FieldNotFound` (2008) — do not fall back to first-match. Empty/whitespace `constantName` / `sourceFile` stay today's `MissingRequiredParam` at Validate even when `line`/`column` are supplied. `line < 1` is `InvalidLineNumber` (1006). `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `constantName` / `typeName` / `removeConstant` / `preview`. Preview describes the inline and writes nothing. This leftover does not invent a second tool.
- `format_document` now honors optional **`preview`**: omitted / false keeps today's apply path (`Formatter.FormatAsync` + commit). `preview: true` formats in memory, returns `RefactoringResult.PreviewResult` with a `PendingChange` (before/after snippets from the unformatted vs formatted text), and writes nothing. If formatting yields identical text, preview still succeeds (empty pending list — no new error code). Empty/whitespace `sourceFile` stays today's `MissingRequiredParam`. Additive with existing `sourceFile`. This leftover does not invent a second tool.
- `analyze_data_flow` now honors optional **`startColumn`** / **`endColumn`**: omitted keeps today's whole-line span (start of `startLine` through end of `endLine` / `TextLine.End`). Do not force column 1 when omitted. `startLine` / `endLine` stay required. Set `startColumn` uses that 1-based column on `startLine` (Roslyn `Character = column - 1`); set `endColumn` uses that 1-based column on `endLine`. One omitted, the other set: omitted `startColumn` still start-of-line; omitted `endColumn` still end-of-line. A column past that line's `TextLine.End` is `InvalidColumnNumber` (1007) — same line-local bound as `analyze_control_flow` / `SymbolResolver` / `extract_method` — so an oversized column cannot leak into later lines. Same-line `startColumn` > `endColumn` is `InvalidRegion`. Combined span must still be start <= end in absolute positions. Statement matching stays today's `span.Contains(s.Span)` — columns that yield empty/no statements stay `InvalidRegion` (no fallback). Empty/whitespace `sourceFile` stays today's `MissingRequiredParam` at Validate even when columns are supplied. `startLine < 1` / `endLine < 1` stays `InvalidLineNumber` (1006). `startColumn < 1` or `endColumn < 1` is `InvalidColumnNumber` (1007). Additive with existing `sourceFile` / `startLine` / `endLine`. This leftover does not invent a second tool.
- `analyze_control_flow` now honors optional **`startColumn`** / **`endColumn`**: omitted keeps today's whole-line span (start of `startLine` through end of `endLine` / `TextLine.End`). Do not force column 1 when omitted. `startLine` / `endLine` stay required. Set `startColumn` uses that 1-based column on `startLine` (Roslyn `Character = column - 1`); set `endColumn` uses that 1-based column on `endLine`. One omitted, the other set: omitted `startColumn` still start-of-line; omitted `endColumn` still end-of-line. A column past that line's `TextLine.End` is `InvalidColumnNumber` (1007) — same line-local bound as `SymbolResolver` / `extract_method` — so an oversized column cannot leak into later lines. Same-line `startColumn` > `endColumn` is `InvalidRegion`. Combined span must still be start <= end in absolute positions. Statement matching stays today's `span.Contains(s.Span)` — columns that yield empty/no statements stay `InvalidRegion` (no fallback). Empty/whitespace `sourceFile` stays today's `MissingRequiredParam` at Validate even when columns are supplied. `startLine < 1` / `endLine < 1` stays `InvalidLineNumber` (1006). `startColumn < 1` or `endColumn < 1` is `InvalidColumnNumber` (1007). Additive with existing `sourceFile` / `startLine` / `endLine`. This leftover does not invent a second tool.
- `introduce_parameter` now honors optional **`column`**: omitted keeps today's start-line equality on the local declaration statement (`StartLinePosition.Line == line-1`), then `variableName` `FirstOrDefault` among `Variables`. Do not force column 1 when omitted. Do not rewrite line-only to covering-span. `Line` stays required. Set with `line` picks the matching `VariableDeclaratorSyntax` whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest covering declarator — same exclusive-end coverage as `encapsulate_field` / `SpanCoversColumn`). When `column` is set, a declarator whose identifier lives on a continuation line is eligible — do not require the declaration statement to start on `line`. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line declarator does not also match the previous one. If `column` is set and nothing covers that position (or the covering declarator is not `variableName`), `SymbolNotFound` (2003) — do not fall back to FirstOrDefault. Line-only with no start-line match keeps today's `SymbolNotFound`. Empty/whitespace `sourceFile` / `variableName` stay today's `MissingRequiredParam` at Validate even when `column` is supplied. `line < 1` stays `InvalidLineNumber` (1006). After the rewrite, keep today's selected declarator — do not rematch by name / stale `SpanStart` / line. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `sourceFile` / `variableName` / `line` / `preview`. Preview describes the promote and writes nothing. This leftover does not invent a second tool.
- `get_code_metrics` now honors optional **`column`**: omitted keeps today's resolution (`ResolveSymbolAsync(..., line, null)` — Line-only uses SymbolResolver's `column ?? 1` walk; file-level when neither `symbolName` nor `line` is set, including when a stray column is supplied). Do not force column 1 when omitted. Do not change the file-level gate (`symbolName || line`). `column` without `line` follows today's SymbolResolver omitted-line + supplied-column path (name-based when `symbolName` is set; `SymbolAmbiguous` when several share the name — do not invent a new omitted-line FirstOrDefault). Set with `line` picks the symbol at that 1-based column (same as `find_callers` / `get_symbol_info`). Empty/whitespace `sourceFile` stays today's missing-param / path errors at Validate even when `column`+`line` is supplied. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `sourceFile` / `symbolName` / `line`. This leftover does not invent a second tool.
- `move_type_to_namespace` now honors optional **`column`**: omitted keeps today's symbolName + optional line pick (start-line equality among top-level types when several share the simple name; `SymbolAmbiguous` when line is omitted and several types share the name; a single match ignores line). Do not force column 1 when omitted. Do not rewrite line-only to covering-span. Do not invent a new omitted-line FirstOrDefault if today throws `SymbolAmbiguous`. Set with `line` picks the matching **top-level** type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest covering type — same exclusive-end coverage as `move_type_to_file` / `encapsulate_field` / `use_base_type` / `SpanCoversColumn`). Nested types stay unmoveable and resolve as `SymbolNotFound` (2003) because they are not top-level candidates. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's omitted-line path); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `SymbolNotFound` (2003) — do not fall back to first-match. Line-only with no start-line match keeps today's `SymbolNotFound`. Empty/whitespace `symbolName` / `sourceFile` / `targetNamespace` stay today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. After resolving the type, the namespace rewrite already keys off the selected declaration — do not rematch by symbolName, stale `SpanStart`, or line after rewrite. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing move_type_to_namespace flags (`sourceFile`, `symbolName`, `line`, `targetNamespace`, `updateFileLocation`, `preview`). Additive with existing `symbolName` / `targetNamespace` / `updateFileLocation` / `preview` / `line`. Preview describes the move and writes nothing. This leftover does not invent a second tool.
- `move_type_to_file` now honors optional **`column`**: omitted keeps today's symbolName + optional line pick (start-line equality among top-level types when several share the simple name; `SymbolAmbiguous` when line is omitted and several types share the name; a single match ignores line). Do not force column 1 when omitted. Do not rewrite line-only to covering-span. Do not invent a new omitted-line FirstOrDefault if today throws `SymbolAmbiguous`. Set with `line` picks the matching **top-level** type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest covering type — same exclusive-end coverage as `encapsulate_field` / `use_base_type` / `push_members_down` / `SpanCoversColumn`). Nested types stay unmoveable (`SymbolIsNested`). When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's omitted-line path); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `SymbolNotFound` (2003) — do not fall back to first-match. Line-only with no start-line match keeps today's `SymbolNotFound`. Empty/whitespace `symbolName` / `sourceFile` / `targetFile` stay today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. After resolving the type, the move already keys off the selected declaration — do not rematch by symbolName, stale `SpanStart`, or line after rewrite. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing move_type_to_file flags (`sourceFile`, `symbolName`, `line`, `targetFile`, `createTargetFile`, `preview`). Additive with existing `symbolName` / `targetFile` / `createTargetFile` / `preview` / `line`. Preview describes the move and writes nothing. This leftover does not invent a second tool.
- `encapsulate_field` now honors optional **`column`**: omitted keeps today's fieldName + optional line pick (`VariableDeclaratorSyntax` `FirstOrDefault` among field declarators when line is omitted — locals and other non-field declarators stay excluded; identifier/declaration span coverage when line is set, including today's `FieldNotFound` when line misses). Do not force column 1 when omitted. Do not invent `SymbolAmbiguous` on omitted line/column. Set with `line` picks the field whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest covering declarator/field — same exclusive-end coverage as `use_base_type` / `push_members_down` / `pull_members_up` / `SpanCoversColumn`). Nested types participate. When `column` is set with `line`, a field whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name fields across lines (today's `FirstOrDefault` after the fieldName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line field does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `FieldNotFound` (2008) — do not fall back to first-match. Line-only with no covering span keeps today's `FieldNotFound` (2008). Empty/whitespace `fieldName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `FieldNotFound` when Find returns null. After the rewrite rematches the selected field, the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale field — do not rematch by fieldName, stale `SpanStart`, or line after rewrite. Do not invent a post-rewrite FirstOrDefault rematch. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing encapsulate_field flags (`sourceFile`, `fieldName`, `line`, `propertyName`, `readOnly`, `updateReferences`, `preview`). Additive with existing `fieldName` / `propertyName` / `readOnly` / `updateReferences` / `preview` / `line`. Preview describes the encapsulate and writes nothing. This leftover does not invent a second tool.
- `use_base_type` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`TypeDeclarationSyntax` `PickOmittedLineMatch` when line is omitted — `matches[0]` when the name has no `.` or there is a single match, else semantic `TypeNameMatches` for FQN; enum and `DelegateDeclarationSyntax` do not participate; identifier/declaration span coverage when line is set, including today's `PickOmittedLineMatch` fallback when line misses, plus today's extra covering `BaseTypeDeclarationSyntax` enum / `DelegateDeclarationSyntax` candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` FirstOrDefault (that would add enums). Do not add enums or delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `push_members_down` / `pull_members_up` / `extract_base_class` / `extract_interface` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra covering enum / `DelegateDeclarationSyntax` candidates so a covering non-class/struct/interface reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's `PickOmittedLineMatch` after the typeName filter, `TypeDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. Line-only with no covering span keeps today's `PickOmittedLineMatch` fallback (enums and delegates stay out of that omitted-line fallback). Empty/whitespace `typeName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `TypeNotFound` when Find returns null — do not invent `SymbolNotFound` for this operation. After resolving the derived symbol, reference discovery already keys off `FindTypeReferencesAsync` — do not rematch the derived declaration by typeName, stale `SpanStart`, or line after rewrite. Do not invent a post-rewrite FirstOrDefault rematch. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing use_base_type flags (`sourceFile`, `typeName`, `line`, `targetBaseType`, `preview`). Additive with existing `typeName` / `targetBaseType` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `push_members_down` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`TypeDeclarationSyntax` `FirstOrDefault` when line is omitted — enum and `DelegateDeclarationSyntax` do not participate; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses, plus today's extra covering `BaseTypeDeclarationSyntax` enum / `DelegateDeclarationSyntax` candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` FirstOrDefault (that would add enums). Do not add enums or delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `pull_members_up` / `extract_base_class` / `extract_interface` / `generate_tostring` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra covering enum / `DelegateDeclarationSyntax` candidates so a covering non-class/struct/interface reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter, `TypeDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. Line-only with no covering span keeps today's `TypeDeclarationSyntax` first-match fallback. Empty/whitespace `typeName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `TypeNotFound` when Find returns null — do not invent `SymbolNotFound` for this operation. After the push (source rewrite + members added to derived types), the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span / annotation rather than FirstOrDefault by typeName. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing push_members_down flags (`sourceFile`, `typeName`, `line`, `members`, `targetDerivedTypes`, `leaveAbstract`, `preview`). Additive with existing `typeName` / `members` / `targetDerivedTypes` / `leaveAbstract` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `pull_members_up` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`TypeDeclarationSyntax` `FirstOrDefault` when line is omitted — enum and `DelegateDeclarationSyntax` do not participate; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses, plus today's extra covering `BaseTypeDeclarationSyntax` enum / `DelegateDeclarationSyntax` candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` FirstOrDefault (that would add enums). Do not add enums or delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `extract_base_class` / `extract_interface` / `generate_tostring` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra covering enum / `DelegateDeclarationSyntax` candidates so a covering non-class/struct/interface reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter, `TypeDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. Line-only with no covering span keeps today's `TypeDeclarationSyntax` first-match fallback. Empty/whitespace `typeName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `TypeNotFound` when Find returns null — do not invent `SymbolNotFound` for this operation. After the pull (derived members removed / members added to the target), the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span / annotation rather than FirstOrDefault by typeName. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing pull_members_up flags (`sourceFile`, `typeName`, `line`, `members`, `targetBaseType`, `makeAbstract`, `preview`). Additive with existing `typeName` / `members` / `targetBaseType` / `makeAbstract` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `extract_base_class` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`ClassDeclarationSyntax` `FirstOrDefault` when line is omitted — enum, struct, interface, and `DelegateDeclarationSyntax` do not participate; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses, plus today's extra covering `BaseTypeDeclarationSyntax` enum/struct/interface / `DelegateDeclarationSyntax` candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` or `TypeDeclarationSyntax` FirstOrDefault (that would add enums/structs/interfaces). Do not add enums, structs, interfaces, or delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `extract_interface` / `generate_tostring` / `implement_abstract` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra covering enum/struct/interface / `DelegateDeclarationSyntax` candidates so a covering non-class reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter, `ClassDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. Line-only with no covering span keeps today's `ClassDeclarationSyntax` first-match fallback. Empty/whitespace `typeName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `TypeNotFound` when Find returns null — do not invent `SymbolNotFound` for this operation. After the extract (derived `: BaseClassName` / same-file or separate-file), the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span / annotation rather than FirstOrDefault by typeName. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing extract_base_class flags (`sourceFile`, `typeName`, `line`, `baseClassName`, `members`, `targetFile`, `separateFile`, `makeAbstract`, `preview`). Additive with existing `typeName` / `baseClassName` / `members` / `targetFile` / `separateFile` / `makeAbstract` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `extract_interface` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`TypeDeclarationSyntax` `FirstOrDefault` when line is omitted — enum and `DelegateDeclarationSyntax` do not participate; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses, plus today's extra covering enum / `DelegateDeclarationSyntax` candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` FirstOrDefault (that would add enums). Do not add enums or delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_tostring` / `implement_abstract` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra covering enum / `DelegateDeclarationSyntax` candidates so a covering enum or delegate reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter, `TypeDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. Empty/whitespace `typeName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `TypeNotFound` when Find returns null — do not invent `SymbolNotFound` for this operation. After same-file extract and/or `addInterfaceToType` rewrites the source document, the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Do not rematch FirstOrDefault by typeName after rewrite. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing extract_interface flags (`sourceFile`, `typeName`, `line`, `interfaceName`, `members`, `targetFile`, `separateFile`, `addInterfaceToType`, `preview`). Additive with existing `typeName` / `interfaceName` / `members` / `targetFile` / `separateFile` / `addInterfaceToType` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_tostring` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`TypeDeclarationSyntax` `FirstOrDefault` when line is omitted — enum and `DelegateDeclarationSyntax` do not participate; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses, plus today's extra covering enum / `DelegateDeclarationSyntax` candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` FirstOrDefault (that would add enums). Do not add enums or delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_constructor` / `implement_abstract` / `generate_overrides` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra covering enum / `DelegateDeclarationSyntax` candidates so a covering enum or delegate reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter, `TypeDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. Empty/whitespace `typeName` stays today's `MissingRequiredParam` at Validate even when `column`+`line` is supplied. A name that is not present at all stays today's `TypeNotFound` when Find returns null — do not invent `SymbolNotFound` for this operation. After `replaceExisting` removes ToString from an earlier same-file partial, the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing generate_tostring flags (`sourceFile`, `typeName`, `line`, `fields`, `includeProperties`, `format`, `includeInheritedMembers`, `replaceExisting`, `callSuper`, `preview`). Additive with existing `typeName` / `fields` / `includeProperties` / `format` / `includeInheritedMembers` / `replaceExisting` / `callSuper` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `implement_abstract` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`BaseTypeDeclarationSyntax` `FirstOrDefault` when line is omitted, including enum; `DelegateDeclarationSyntax` is not in that omitted-line set; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses, plus today's extra `DelegateDeclarationSyntax` covering candidates). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `TypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault. Do not add delegates to the omitted-line set. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_property` / `generate_overrides` / `SpanCoversColumn`). Nested types participate. When line is set, keep today's extra `DelegateDeclarationSyntax` covering candidates so a covering delegate reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter, `BaseTypeDeclarationSyntax` only); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. An empty typeName candidate set stays today's `SymbolNotFound` even when `column`+`line` is supplied. After `replaceExisting` removes members from an earlier same-file partial, the selected declaration is recovered by the existing per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing implement_abstract flags (`sourceFile`, `typeName`, `line`, `members`, `throwNotImplemented`, `replaceExisting`, `preview`). Additive with existing `typeName` / `members` / `throwNotImplemented` / `replaceExisting` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_property` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`BaseTypeDeclarationSyntax` `FirstOrDefault` when line is omitted, including enum; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `TypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_constructor` / `generate_overrides` / `SpanCoversColumn`). Nested types participate. Unsupported `BaseTypeDeclarationSyntax` candidates (enum / delegate) stay in the set so a covering enum reaches `InvalidSymbolKind` rather than retargeting a later class. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. An empty typeName candidate set stays today's `SymbolNotFound` even when `column`+`line` is supplied. After `replaceExisting` removes a property from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing generate_property flags (`sourceFile`, `typeName`, `line`, `propertyName`, `propertyType`, `fieldName`, `visibility`, `initOnly`, `replaceExisting`, `preview`). Additive with existing `typeName` / `propertyName` / `propertyType` / `fieldName` / `visibility` / `initOnly` / `replaceExisting` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_constructor` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`FirstOrDefault` when line is omitted; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_equals_hashcode` / `generate_overrides` / `implement_interface` / `SpanCoversColumn`). Nested types participate. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. After `replaceExisting` removes a constructor from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing generate_constructor flags (`sourceFile`, `typeName`, `line`, `members`, `includeProperties`, `includeInheritedMembers`, `addNullChecks`, `replaceExisting`, `visibility`, `copyConstructor`, `classBaseCopy`, `callBase`, `preview`). Additive with existing `typeName` / `members` / `includeProperties` / `includeInheritedMembers` / `addNullChecks` / `replaceExisting` / `visibility` / `copyConstructor` / `classBaseCopy` / `callBase` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_equals_hashcode` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`FirstOrDefault` when line is omitted; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_overrides` / `implement_interface` / `add_null_checks` / `SpanCoversColumn`). Nested types participate. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. After `replaceExisting` removes a member from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing generate_equals_hashcode flags (`sourceFile`, `typeName`, `line`, `fields`, `includeProperties`, `implementIEquatable`, `generateOperators`, `replaceExisting`, `useHashCodeCombine`, `callSuper`, `includeInheritedMembers`, `preview`). Additive with existing `typeName` / `fields` / `includeProperties` / `implementIEquatable` / `generateOperators` / `replaceExisting` / `useHashCodeCombine` / `callSuper` / `includeInheritedMembers` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `implement_interface` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`FirstOrDefault` when line is omitted; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses). Do not force column 1 when omitted. Do not change omitted-line/omitted-column to `BaseTypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_overrides` / `add_null_checks` / `SpanCoversColumn`). Nested types participate. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. After `replaceExisting` removes a member from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). If `GetDocument(oldTree)` misses after `WithSyntaxRoot`, look up by file path and rematch by span. `column < 1` is `InvalidColumnNumber` (1007). Do not rename or restyle existing implement_interface flags (`sourceFile`, `typeName`, `line`, `interfaceName`, `explicitImplementation`, `members`, `throwNotImplemented`, `replaceExisting`, `preview`). Additive with existing `typeName` / `interfaceName` / `explicitImplementation` / `members` / `throwNotImplemented` / `replaceExisting` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_overrides` now honors optional **`column`**: omitted keeps today's typeName + optional line pick (`FirstOrDefault` when line is omitted; identifier/declaration span coverage when line is set, including today's first-match fallback when line misses). Do not force column 1 when omitted. Set with `line` picks the type whose identifier or declaration span covers that 1-based column (identifier preferred, then smallest containing type — same exclusive-end coverage as `add_null_checks` / `change_signature` / `SpanCoversColumn`). Nested types participate. When `column` is set with `line`, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name types across lines (today's first-match after the typeName filter); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line type does not also match the previous declaration. If `column` is set with `line` and nothing covers that position, `TypeNotFound` — do not fall back to first-match. After `replaceExisting` removes an override from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `typeName` / `members` / `callBase` / `replaceExisting` / `preview` / `line`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `encapsulate_field` now honors optional **`line`**: omitted keeps today's fieldName `VariableDeclaratorSyntax` `FirstOrDefault` pick among field declarators (do not fail when several same-named fields exist; do not invent `SymbolAmbiguous`). Set picks the field whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest covering declarator/field — same exclusive-end coverage as `use_base_type` / `push_members_down` / `pull_members_up` / `SpanCoversLine`). Nested types participate. Locals / non-field declarators stay excluded. When `line` is set, a field whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If `line` is set and nothing covers this line, `FieldNotFound` (2008) — do not fall back to first-match. After the rewrite rematches the selected field (today's `First` by `fieldName` / containing type name), the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale field — do not reuse a pre-rewrite `SpanStart` or line (those shift). Same-file outer/sibling references are classified by semantic containment in the selected field's containing type (innermost `TypeDeclarationSyntax` vs declaring syntax), not file-path equality, so a `nested.name` caller in the same file is external and rewrites to the property when `updateReferences` is true; same-type internal uses stay on the field. Same-file external identifiers are annotated before the field rewrite so a later property insert cannot rematch a stale span. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing encapsulate_field flags (`sourceFile`, `fieldName`, `propertyName`, `readOnly`, `updateReferences`, `preview`). Additive with existing `propertyName` / `readOnly` / `updateReferences` / `preview`. Preview describes the encapsulate and writes nothing. This leftover does not invent a second tool.
- `use_base_type` now honors optional **`line`**: omitted keeps today's typeName `TypeDeclarationSyntax` first-match (`matches[0]` when the name has no `.` or there is a single match; otherwise semantic `TypeNameMatches` for FQN). Do not fail when several same-named types exist and line is omitted. Do not change omitted-line to `BaseTypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault — that would invent a new enum pick or drop structs/interfaces; `DelegateDeclarationSyntax` is not a `TypeDeclarationSyntax`, so a leading delegate or enum plus a later same-named class still picks the class. Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `push_members_down` / `pull_members_up` / `extract_base_class` / `extract_interface` / `SpanCoversLine`). Nested types, enums, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class (those unsupported targets reach existing `InvalidSymbolKind` validation). When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match / FQN semantic pick rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After resolving the derived symbol, reference discovery already keys off `FindTypeReferencesAsync` — do not invent a post-rewrite rematch of the derived declaration by stale `SpanStart` or `line`. Do not add a static annotation. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing use_base_type flags (`sourceFile`, `typeName`, `targetBaseType`, `preview`). Additive with existing `typeName` / `targetBaseType` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `push_members_down` now honors optional **`line`**: omitted keeps today's typeName `TypeDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named types exist; do not change omitted-line to `BaseTypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault — that would invent a new enum pick or drop structs/interfaces; `DelegateDeclarationSyntax` is not a `TypeDeclarationSyntax`, so a leading delegate or enum plus a later same-named class still picks the class). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `pull_members_up` / `extract_base_class` / `extract_interface` / `generate_tostring` / `implement_abstract` / `generate_property` / `SpanCoversLine`). Nested types, enums, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class (those unsupported targets reach existing `InvalidSymbolKind` validation). When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After the push (source rewrite + members added to derived types), the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's rematch `GetTypeDeclarationAsync` by symbol is not enough by itself for the initial source pick. If `GetDocument(oldTree)` misses after the annotation rewrite, look up by file path and rematch by span. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing push_members_down flags (`sourceFile`, `typeName`, `members`, `targetDerivedTypes`, `leaveAbstract`, `preview`). Additive with existing `typeName` / `members` / `targetDerivedTypes` / `leaveAbstract` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `pull_members_up` now honors optional **`line`**: omitted keeps today's typeName `TypeDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named types exist; do not change omitted-line to `BaseTypeDeclarationSyntax` / `ClassDeclarationSyntax` FirstOrDefault — that would invent a new enum pick or drop structs/interfaces; `DelegateDeclarationSyntax` is not a `TypeDeclarationSyntax`, so a leading delegate or enum plus a later same-named class still picks the class). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `extract_base_class` / `extract_interface` / `generate_tostring` / `implement_abstract` / `generate_property` / `SpanCoversLine`). Nested types, enums, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class (those unsupported targets reach existing `InvalidSymbolKind` validation). When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After the pull (derived rewrite + members added to the target), the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's rematch `FindTypeInRoot` by preferred span is not enough by itself. If `GetDocument(oldTree)` misses after the annotation rewrite, look up by file path and rematch by span. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing pull_members_up flags (`sourceFile`, `typeName`, `members`, `targetBaseType`, `makeAbstract`, `preview`). Additive with existing `typeName` / `members` / `targetBaseType` / `makeAbstract` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `extract_base_class` now honors optional **`line`**: omitted keeps today's typeName `ClassDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named classes exist; do not change omitted-line to `BaseTypeDeclarationSyntax` / `TypeDeclarationSyntax` FirstOrDefault — that would invent a new enum/struct/interface pick; `DelegateDeclarationSyntax` is not a `BaseTypeDeclarationSyntax`, so a leading delegate, enum, struct, or interface plus a later same-named class still picks the class). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `extract_interface` / `generate_tostring` / `implement_abstract` / `generate_property` / `SpanCoversLine`). Nested types, enums, structs, interfaces, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class (those unsupported targets reach existing `InvalidSymbolKind` validation). When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After the extract (and when adding `: BaseClassName` to the derived type), the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's rematch `.First(t => t.Identifier.Text == typeName)` is not enough. If `GetDocument(oldTree)` misses after the annotation rewrite, look up by file path and rematch by span. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing extract_base_class flags (`baseClassName`, `members`, `targetFile`, `separateFile`, `makeAbstract`, `preview`). Additive with existing `typeName` / `baseClassName` / `members` / `targetFile` / `separateFile` / `makeAbstract` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `extract_interface` now honors optional **`line`**: omitted keeps today's typeName `TypeDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named types exist; do not change omitted-line to `BaseTypeDeclarationSyntax` FirstOrDefault — that would invent a new enum/delegate pick; `DelegateDeclarationSyntax` is not a `TypeDeclarationSyntax`, so a leading delegate or enum plus a later same-named class still picks the class). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_tostring` / `implement_abstract` / `generate_property` / `SpanCoversLine`). Nested types, enums, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class (those unsupported targets reach existing `InvalidSymbolKind` validation). When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After same-file extract and/or `addInterfaceToType` rewrites the source document, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's `AddInterfaceToBaseListAsync` `FirstOrDefault` by `typeName` is not enough. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing extract_interface flags (`members`, `targetFile`, `separateFile`, `addInterfaceToType`, `preview`). Additive with existing `typeName` / `interfaceName` / `members` / `targetFile` / `separateFile` / `addInterfaceToType` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_tostring` now honors optional **`line`**: omitted keeps today's typeName `TypeDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named types exist; do not change omitted-line to `BaseTypeDeclarationSyntax` FirstOrDefault — that would invent a new enum/delegate pick; `DelegateDeclarationSyntax` is not a `TypeDeclarationSyntax`, so a leading delegate or enum plus a later same-named class still picks the class). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `implement_abstract` / `generate_property` / `SpanCoversLine`). Nested types, enums, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class (those unsupported targets reach existing `InvalidSymbolKind` validation). When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes ToString from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's `FindTypeDeclaration(root, typeName, preferredSpanStart)` is not enough. If `GetDocument(oldTree)` misses after the annotation rewrite, look up by file path and rematch by span. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing tostring flags (`fields`, `includeProperties`, `format`, `includeInheritedMembers`, `replaceExisting`, `callSuper`, `preview`). Additive with existing `typeName` / `fields` / `includeProperties` / `format` / `includeInheritedMembers` / `replaceExisting` / `callSuper` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `implement_abstract` now honors optional **`line`**: omitted keeps today's typeName `BaseTypeDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named types exist; an earlier same-named enum still hits `InvalidSymbolKind`; `DelegateDeclarationSyntax` is not a `BaseTypeDeclarationSyntax`, so a leading delegate plus a later same-named class still picks the class). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_property` / `generate_constructor` / `generate_equals_hashcode` / `implement_interface` / `SpanCoversLine`). Nested types, enums, and `DelegateDeclarationSyntax` participate when line is set so a covering enum or delegate is not silently retargeted to a later class. When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes members from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's `FindTypeDeclaration(root, typeName, preferredSpanStart)` is not enough (it also still scans `TypeDeclarationSyntax` only). `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing implement_abstract flags. Additive with existing `typeName` / `members` / `throwNotImplemented` / `replaceExisting` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_property` now honors optional **`line`**: omitted keeps today's typeName `BaseTypeDeclarationSyntax` `FirstOrDefault` pick (do not fail when several same-named types exist; an earlier same-named enum/delegate still hits `InvalidSymbolKind`). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_constructor` / `generate_equals_hashcode` / `implement_interface` / `SpanCoversLine`). Nested types and unsupported `BaseTypeDeclarationSyntax` candidates (enum / delegate) participate so a covering enum is not silently retargeted to a later class. When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes a property from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). Today's `FindTypeDeclaration(root, typeName, preferredSpanStart)` is not enough. `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing property flags. Additive with existing `typeName` / `propertyName` / `propertyType` / `fieldName` / `visibility` / `initOnly` / `replaceExisting` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_constructor` now honors optional **`line`**: omitted keeps today's typeName `FirstOrDefault` pick (do not fail when several same-named types exist). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_equals_hashcode` / `implement_interface` / `SpanCoversLine`). Nested types participate. When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes a constructor from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename or restyle existing constructor flags. Additive with existing `typeName` / `members` / `includeProperties` / `includeInheritedMembers` / `addNullChecks` / `replaceExisting` / `visibility` / `copyConstructor` / `classBaseCopy` / `callBase` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_equals_hashcode` now honors optional **`line`**: omitted keeps today's typeName `FirstOrDefault` pick (do not fail when several same-named types exist). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_overrides` / `implement_interface` / `SpanCoversLine`). Nested types participate. When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes a member from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Do not rename `Fields` to `members`. Additive with existing `typeName` / `fields` / `includeProperties` / `implementIEquatable` / `generateOperators` / `replaceExisting` / `useHashCodeCombine` / `callSuper` / `includeInheritedMembers` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `implement_interface` now honors optional **`line`**: omitted keeps today's typeName `FirstOrDefault` pick (do not fail when several same-named types exist). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `generate_overrides` / `SpanCoversLine`). Nested types participate. When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes a member from an earlier same-file partial, the selected declaration is recovered by a per-execution syntax annotation (not a static field) and the annotation is stripped before commit so a reused workspace cannot recover a stale type — do not reuse a pre-rewrite `SpanStart` or line (those shift). `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Additive with existing `typeName` / `interfaceName` / `explicitImplementation` / `members` / `throwNotImplemented` / `replaceExisting` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `generate_overrides` now honors optional **`line`**: omitted keeps today's typeName `FirstOrDefault` pick (do not fail when several same-named types exist). Set picks the type whose identifier or declaration span covers that 1-based line (identifier preferred, then smallest containing type — same exclusive-end coverage as `add_null_checks` / `SpanCoversColumn`). Nested types participate. When `line` is set, a type whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. If nothing covers this line, keep today's first-match rather than inventing a not-found; do not silently pick the first when a covering node exists elsewhere. After `replaceExisting` removes an override from an earlier same-file partial, the selected declaration is recovered by syntax annotation — do not reuse a pre-rewrite `SpanStart` or line (those shift). `line < 1` is `InvalidLineNumber` (1006). Do not add `column` in this leftover. Additive with existing `typeName` / `members` / `callBase` / `replaceExisting` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `add_null_checks` now honors optional **`column`**: omitted keeps today's methodName + optional line start-line pick (`FirstOrDefault` whose declaration start line matches, else today's silent `First()` fallback when line misses). Set picks the smallest method or constructor whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `change_signature` / `convert_property`). When `column` is set, a method whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name methods across lines (today's first-match); do not substitute each candidate's own start line. Constructors participate (today's `FindMethod` already includes them). `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line method does not also match the previous declaration. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `methodName` / `line` / `style` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `change_signature` now honors optional **`column`**: omitted keeps today's methodName and/or line start-line pick (`FirstOrDefault` whose declaration start line matches when more than one method shares the name). Set picks the smallest method whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `convert_property` / `convert_to_async`). When `column` is set, a method whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name methods across lines (today's `SymbolAmbiguous`); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line method does not also match the previous declaration. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `methodName` / `line` / `parameters` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `convert_property` now honors optional **`column`**: omitted keeps today's propertyName and/or line start-line pick (`FirstOrDefault` whose declaration start line matches). Set picks the smallest property whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `convert_to_pattern_matching` / `convert_to_block_body`). When `column` is set, a property whose identifier lives on a continuation line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name properties across lines (today's first-match); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line property does not also match the previous declaration. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `propertyName` / `line` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `convert_to_pattern_matching` now honors optional **`column`**: omitted keeps today's first-on-the-line pick (switch statement, then if/is chain, whose start line matches). Set picks the smallest switch or if whose span covers that 1-based column (same span-covers-column idea as `convert_to_interpolated_string` / `convert_to_block_body`). When `column` is set, a statement whose keyword lives on a continuation line is eligible — do not require the statement to start on `line`. Do not fall back to the start-line pick when no covering node exists. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line statement does not also match the previous switch/if. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `line` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `convert_to_block_body` now honors optional **`column`**: omitted keeps today's `FindMember` pick (`memberName` and/or `line`; smallest containing node on the line). Set picks the member whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `convert_expression_body` / `convert_to_async`). When `column` is set, a member whose declaration continues onto that line is eligible — do not require the declaration to start on `line`. `column` without `line` cannot disambiguate same-indent same-name members across lines (today's first-match); do not substitute each candidate's own start line. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line member does not also match the previous declaration. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `memberName` / `line` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `inline_variable` now honors optional **`column`**: omitted keeps today's variableName + optional line start-line pick (match `variableName`; if more than one, require `line`; first start-line match). Set picks the declaration whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `convert_to_async` / `convert_expression_body`). When `column` is set, a declaration whose identifier lives on a continuation line is eligible — the start-line filter is only for omitted `column`. Multiple same-name locals still require `line` even if `column` is set (`SymbolAmbiguous` 2004) so equally aligned locals are not silently picked. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line declaration does not also match the previous one. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `variableName` / `line` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `encapsulate_field` now honors **`updateReferences`**: omitted / true keeps today's rewrite of external references to the new property (`SymbolFinder.FindReferencesAsync` + identifier replace). `false` still encapsulates (private field + property) and does not redirect callers to the property. When the backing field is renamed for a case-only collision (`name` → `_name`), those external identifiers still follow the new field name so they do not point at a symbol that no longer exists; when the field is not renamed, external callers stay unchanged. Same-class references stay on the field either way (BR-EF1.5). Additive with existing `propertyName` / `readOnly` / `preview`. Preview describes whether references will be updated (including a renamed backing field) and writes nothing. This leftover does not invent a second tool.
- `convert_to_async` now honors optional **`column`**: omitted keeps today's MethodName + Line pick (match `methodName`; if more than one, require `line`; first start-line match). Set picks the method whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `convert_expression_body` / `convert_to_interpolated_string`). When `column` is set, a method whose declaration continues onto that line is eligible — the start-line filter is only for omitted `column`. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line method does not also match the previous declaration. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `methodName` / `line` / `renameToAsync` / `updateCallers` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `convert_to_interpolated_string` now honors optional **`column`**: omitted keeps today's first-on-the-line pick (`string.Format` invocation, then concatenation, whose start line matches). Set picks the Format invocation or concatenation whose span covers that 1-based column (same span-covers-column idea as `convert_expression_body` / `convert_foreach_linq`). A 3+ operand concatenation is converted from the **outer** matching `+` chain (`OuterConcatenation` / flatten `outerConcat`) so a column (or omitted first match) on an inner operand of `"Hello " + name + suffix` keeps `suffix`. Adjacent independent concatenations on one line stay distinct. Do not force spec default 1 when omitted — indented expressions still convert. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent same-line expression does not also match the previous Format/concat. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `line` / `preview`. Preview describes the rewrite and writes nothing. This leftover does not invent a second tool.
- `convert_expression_body` now honors optional **`column`**: omitted keeps today's `FindMember` pick (`memberName` and/or `line`; first match on the line). Set picks the member whose identifier or declaration span covers that 1-based column (same span-covers-column idea as `convert_foreach_linq`). When `column` is set, a member whose declaration continues onto that line is eligible — the start-line filter is only for omitted `column`. `FileLinePositionSpan.EndLinePosition` is exclusive, so a column on an adjacent member does not also match the previous declaration. `column < 1` is `InvalidColumnNumber` (1007). Additive with existing `memberName` / `line` / `direction` / `preview`. Preview describes the rewrite and writes nothing. Direction (`ToExpressionBody` / `ToBlockBody`) is unchanged. This leftover does not invent a `convert_to_expression_body` tool.
- `convert_foreach_linq` now honors **`preferQuerySyntax`** and optional **`column`**: omitted / false keeps today's method syntax (`.Where().Select().ToList()`). `true` emits query syntax (`from … where … select`) for filter, project, filter+project, and ToList after a query. An explicitly typed foreach element is preserved (`from string item in …`) so the foreach conversion still compiles; `var` stays untyped. Patterns that cannot be expressed as query syntax (`Any` / `All` / `FirstOrDefault` / Count-only and similar) keep method syntax rather than inventing invalid query syntax. `column` (optional; spec default 1) picks the foreach whose keyword covers that column on the given line; omitted keeps today's first-foreach-on-the-line pick so indented statements still convert. Additive with existing `line` / `preview`. Preview describes the rewrite and writes nothing.
- `convert_to_async` now honors **`updateCallers`**: omitted / false keeps today's convert of the selected method (and `renameToAsync` identifier rewrites at call sites) without wrapping callers in `await` or making them async. `true` wraps already-async callers in `await` so the edited solution compiles. A still-synchronous caller is not given a bare `await` — that site is skipped and reported (`callersSkipped`) rather than rewriting the rest of the call graph. Additive with `renameToAsync`, `line`, and `preview`. Preview describes caller updates (or that none will happen) and writes nothing. Spec listed 3090/3091; those codes already ship as `MultipleAssignments` / `UsedInRefContext`. Existing convert-to-async codes are 3080–3082; no new reject was required (sync callers are skipped, not failed).

- `extract_base_class` now honors **`makeAbstract`** for methods, properties, events, and indexers: when the flag is true, extracted members become abstract on the new base class and the derived type keeps an `override` implementation (`ConvertToAbstract` / `AddOverrideModifier` shared with `pull_members_up` via `HierarchyAbstractMemberRewriter`). `CanBeAbstract` matches hierarchy ops — static / explicit-interface members cannot be abstract (`MemberNotMoveable`). An indexer or property with an explicit private accessor is `MemberNotMoveable` (CS0621 / CS0546). Fields still move as concrete. `makeAbstract: false` stays as today (move, derived loses the member). The new base is same-assembly. Preview describes the abstract members and leftover overrides and writes nothing.
- `pull_members_up` now honors **CS0507** for methods, properties, events, and indexers on `makeAbstract` overrides: `AddOverrideModifier` runs the same `ReduceOverrideAccessibility` / accessor reduction already used by `push_members_down` `leaveAbstract` (`#177` / `#178`) via shared `OverrideAccessibilityReducer`, so a cross-assembly `protected internal` method, property, event, or indexer is emitted as `protected override` (and a more-restricted `protected internal` accessor as `protected`). Accessors compare against that reduced visibility so a cross-assembly `protected internal` property with a `protected` setter does not emit a redundant `protected set` (CS0273). Same-assembly `protected internal` stays `protected internal`. `makeAbstract: false`, interface targets, preview, and other member kinds stay as they are. Preview describes the override and writes nothing.
- `push_members_down` now honors **CS0507** for methods, properties, and events on `leaveAbstract` overrides: `AddOverrideModifier` runs the same `ReduceCrossAssemblyOverrideAccessibility` / accessor reduction already used for indexers (`#174`) via shared `ReduceOverrideAccessibility`, so a cross-assembly `protected internal` method, property, or event is emitted as `protected override` (and a more-restricted `protected internal` accessor as `protected`). Accessors compare against that reduced visibility so a cross-assembly `protected internal` property with a `protected` setter does not emit a redundant `protected set` (CS0273). Same-assembly `protected internal` stays `protected internal`. Indexer reduction is unchanged. `leaveAbstract: false`, preview, and other member kinds stay as they are. Preview describes the override and writes nothing.
- `generate_overrides` now honors **CS0507** for methods, properties, and indexers: `CreateMethodStub` / `CreatePropertyStub` / `CreateIndexerStub` add five-parameter overloads with `emittingType` and run `SyntaxGenerationHelper.OverrideAccessibility`, so a cross-assembly `protected internal` virtual/abstract member is emitted as `protected override` (and a more-restricted `protected internal` accessor as `protected`). The existing four-parameter overloads stay on the public surface and forward with `emittingType: null` (same-assembly behavior). Same-assembly `protected internal` stays `protected internal`. Events already did this (`#157` / `#158`); `internal` / `private protected` members from another assembly stay unselected (`MemberAnalyzer.IsAccessibleFrom`). `callBase`, `replaceExisting`, and other member kinds stay unchanged. Preview describes the generated override and writes nothing.
- `push_members_down` now honors **indexers**: `EnumerateDeclaredMembers` yields `IndexerDeclarationSyntax` as `this[]`. `FindMembersToPush` matches indexer metadata name (`Item`), Roslyn name (`this[]`), and conventional display (`this[int i]`) via `ImplementInterfaceOperation.MatchesRequestedMember` — same identity forms as `implement_interface` / `extract_interface` / `extract_base_class` / `pull_members_up`. Named push of an indexer moves that `IndexerDeclarationSyntax` onto each selected derived type (parameter list and `RefKind` preserved). The source type loses it unless `leaveAbstract`. `leaveAbstract: true` against a class makes the indexer abstract on the source and keeps an `override` indexer on each derived type (`CanBeAbstract` now requires `CanPushIndexerAsAbstract` for indexers; `ConvertToAbstract` / `AddOverrideModifier` / `StripHierarchyModifiers` now have indexer arms). Static / explicit-interface indexers still cannot be abstract. `leaveAbstract` now emits a space after the inserted `abstract` keyword so the source writes `abstract class` instead of `abstractclass`. Bodyless abstract indexers copied onto a class get throwing accessor bodies (CS0501). Indexer conflicts compare the constructed signature as seen from each derived target. Cross-assembly `leaveAbstract` reduces `protected internal` (member or accessor) to `protected` on the override (CS0507); same-assembly keeps `protected internal`. An indexer with an explicit private accessor on a more-visible indexer is `MemberNotMoveable` because abstract accessors cannot be private (CS0621) and an override cannot keep that accessor if the base drops it (CS0546), matching `pull_members_up`. Push to an interface emits a legal `this[...]` signature (`ConvertToInterfaceMember` indexer arm). Only publicly accessible accessors are emitted on an interface (a private / protected / internal setter does not become a public interface `set;`), matching `extract_interface` / `pull_members_up`. Ordinary properties stay on `PropertyDeclarationSyntax` arms. Methods, fields, and events stay unchanged. Preview describes the indexer and writes nothing.
- `pull_members_up` now honors **indexers**: `EnumerateDeclaredMembers` yields `IndexerDeclarationSyntax` as `this[]`. `FindMembersToPull` matches indexer metadata name (`Item`), Roslyn name (`this[]`), and conventional display (`this[int i]`) via `ImplementInterfaceOperation.MatchesRequestedMember` — same identity forms as `implement_interface` / `extract_interface` / `extract_base_class`. Named pull of an indexer moves that `IndexerDeclarationSyntax` onto the existing editable base class (parameter list and `RefKind` preserved). The derived type loses it, or keeps an `override` when `makeAbstract` is true on a class. Private indexers become protected on the base via `AdjustBaseClassModifiers`, matching methods / properties / fields / events. Pull to an interface emits a legal `this[...]` signature (`ConvertToInterfaceMember` indexer arm) and keeps the implementation on the derived type. Only publicly accessible accessors are emitted on an interface (a private / protected / internal setter does not become a public interface `set;`), matching `extract_interface`. `makeAbstract` rejects an indexer with an explicit private accessor (`MemberNotMoveable`) because abstract accessors cannot be private (CS0621) and an override cannot keep that accessor if the base drops it (CS0546). Explicit-interface indexers (`IFoo.this[...]`) are not pulled onto a base that does not implement the interface (CS0540), matching `extract_base_class` and the existing explicit-interface event reject. Ordinary properties stay on `PropertyDeclarationSyntax` arms. Methods, fields, and events stay unchanged. `makeAbstract` on a class can mark a pulled indexer abstract and leave an override on the derived type (`ConvertToAbstract` / `AddOverrideModifier` indexer arms). Static / explicit-interface indexers still cannot be abstract. Selected indexer overloads are dependency-checked by symbol, not the shared `this[]` name, so a body that uses an unselected derived overload is `MemberDependsOnDerived`. Preview describes the indexer and writes nothing.
- `extract_base_class` now honors **indexers**: `GetMemberName` recognizes `IndexerDeclarationSyntax` as `this[]`. Named extract of an indexer moves that `IndexerDeclarationSyntax` onto the new base class (parameter list and `RefKind` preserved). The derived type loses it and still inherits the new base. `members` matches indexer metadata name (`Item`), Roslyn name (`this[]`), and conventional display (`this[int i]`) — same identity forms as `implement_interface` / `extract_interface`. Explicit-interface indexers (`IFoo.this[...]`) are not extracted — copying them onto a base that does not implement the interface is CS0540. `AdjustMemberAccessibility` also covers indexers: private → protected, matching methods / properties / fields / events; `private protected` becomes a single `protected` (not `protected protected`). Ordinary properties stay on `PropertyDeclarationSyntax` arms. Methods, fields, and events stay unchanged. `makeAbstract` still only marks the new class abstract — it does not invent abstract indexer members (same as today's event rule). `separateFile` / `targetFile` / preview stay additive. Preview describes the indexer and writes nothing.
- `extract_interface` now honors **indexers**: `CreateInterfaceDeclaration` routes `IPropertySymbol.IsIndexer` through `CreateInterfaceIndexer` so a public indexer extracts as a legal `this[...]` interface member (bracketed parameter list and `RefKind`; get/set as declared; `ref` / `ref readonly` kept) instead of a property named `this[]`. Only publicly accessible accessors are emitted (a private setter does not become a public interface `set;`); an init-only setter is emitted as `init;`. Ordinary properties stay on `CreateInterfaceProperty`. Methods and events stay unchanged. `members` matches indexer metadata name (`Item`), Roslyn name (`this[]`), and conventional display (`this[int i]`) — same identity forms as `implement_interface`. Default extract of a type whose public members include an indexer writes a compiling interface; `addInterfaceToType` still compiles. `separateFile` / `targetFile` / `addInterfaceToType` / preview stay additive. Preview describes the indexer and writes nothing.
- `implement_abstract` now honors **cross-assembly event accessibility**: `CreateOverrideModifiers` reduces `protected internal` to `protected` when the abstract member lives in another assembly (CS0507) — same reduction as `SyntaxGenerationHelper.OverrideAccessibility` used by `generate_overrides` / `implement_interface`. Same-assembly `protected internal` stays `protected internal override`. `IsImplementableAbstractMember` now requires `MemberAnalyzer.IsAccessibleFrom` (public / protected / protected-internal always; `internal` / `private protected` only in the same assembly), so a cross-assembly `internal` / `private protected` abstract event is not selected (`NoUnimplementedAbstractMembers` when nothing else is missing). Methods and properties share the same CS0507 reduction via `CreateOverrideModifiers`; property/indexer accessors compare against that reduced visibility so a cross-assembly `protected internal` property with a `protected` setter does not emit a redundant `protected set` (CS0273). Existing same-assembly event paths (public / protected / protected internal) stay unchanged. Preview describes the event and writes nothing. Additive with `replaceExisting`, `members`, `throwNotImplemented`, and `preview`. `generate_overrides` / `implement_interface` / `extract_interface` / hierarchy move ops stay unchanged.
- `push_members_down` now honors **events**: `EnumerateDeclaredMembers` already yields field-like `EventFieldDeclarationSyntax` (by declarator name) and accessor-style `EventDeclarationSyntax`; `IsSupportedMember` / `IsInterfaceCompatible` include `IEventSymbol`. Default push of a named event moves that event onto each selected derived type (field-like or accessor-style). The base type loses it unless `leaveAbstract`. Multi-variable event fields (`event EventHandler Changed, Other`) drop only the selected declarator from the base and copy only that declarator onto each derived type — unrelated declarators stay on the base (`IsolateMemberSyntax` / `TryKeepRemainingDeclarators`). `leaveAbstract: true` against a class makes the event abstract on the base and keeps an `override event` on each derived type (`CanBeAbstract` now includes non-static, non-explicit-interface `IEventSymbol`; `ConvertToAbstract` / `AddOverrideModifier` now have event arms; do not throw `MemberNotMoveable` for ordinary instance events). An event that already overrides an ancestor becomes `abstract override` on the base (`ToAbstractModifiers` keeps `override` for methods/properties/events). `leaveAbstract` rejects with `MemberNotMoveable` when the source type raises the event (field-like `Invoke` / raise — no raiser relocation) or when a concrete derived type is not among the push targets (would be CS0534). Static events and explicit interface event implementations still fail with `MemberNotMoveable` (they cannot become a legal abstract + override pair). Interface source / interface target paths keep today's event conversion arms. Methods, properties, and fields stay unchanged except the shared `abstract override` modifier preserve. Preview describes the pushed event and writes nothing.
- `pull_members_up` now honors **events**: `EnumerateDeclaredMembers` already yields field-like `EventFieldDeclarationSyntax` (by declarator name) and accessor-style `EventDeclarationSyntax`; `IsSupportedMember` / `IsInterfaceCompatible` include `IEventSymbol`. Default pull of a named event moves that event onto the existing base class (field-like or accessor-style). The derived type loses it. Accessor-style events become `virtual` on the base, matching methods / properties. Field-like events keep field-like form (no invented `virtual` on an event field). Multi-variable event fields (`event EventHandler Changed, Other`) drop only the selected declarator from the derived type and copy only that declarator onto the base — unrelated declarators stay on the derived type (same class of leftover as `extract_base_class` / `push_members_down` `IsolateMemberSyntax`). Private events become protected on the base via `AdjustBaseClassModifiers`, matching methods / properties / fields. `makeAbstract: true` against a base class makes the event abstract on the base and keeps an `override event` on the derived type (`ConvertToAbstract` / `AddOverrideModifier` now have event arms; do not throw `MemberNotMoveable`). Interface targets keep today's keep-implementation-on-derived path: event signature on the interface, implementation stays on the derived type. Methods, properties, and fields stay unchanged. Preview describes the pulled event and writes nothing.
- `extract_base_class` now honors **events**: `GetMemberName` recognizes field-like `EventFieldDeclarationSyntax` by declarator name (same spirit as fields); accessor-style `EventDeclarationSyntax` stays as today. Default extract of a named event moves that event onto the new base class (field-like or accessor-style). The derived type loses it and still inherits the new base. Multi-variable event fields (`event EventHandler Changed, Other`) drop only the selected declarator from the derived type and copy only that declarator onto the base — unrelated declarators stay on the derived type (same class of leftover as `implement_interface` / `generate_overrides`). `AdjustMemberAccessibility` also covers events: private → protected, matching methods / properties / fields. `MemberAnalyzer.CanMoveToBase` / `GetMembersForBaseClass` include non-private `IEventSymbol` (same accessibility gate as properties). `makeAbstract` still only marks the new class abstract — it does not invent abstract event members. Methods, properties, and fields stay unchanged. Preview describes the extracted event and writes nothing.
- `generate_overrides` now discovers and generates inherited virtual / abstract / override (non-sealed) **events**: `MemberAnalyzer.IsOverridable` / `GetOverridableMembers` include `IEventSymbol` and track already-overridden events (`IEventSymbol { IsOverride: true }`) in `alreadyOverridden`. Default / `replaceExisting: false` emits missing overridable events as `override event` stubs with empty add/remove (same body shape as `ImplementAbstractOperation.CreateEventStub` / `SyntaxGenerationHelper.CreateEventStub`). Accessibility matches the base event (not hardcoded `public` — CS0507); a cross-assembly `protected internal` event is emitted as `protected` (CS0507). `IsOverridable` also requires the member to be accessible from the derived type (public / protected / protected-internal always; `internal` / `private protected` only in the same assembly — same switch as constructor / equals inherited collection), so a cross-assembly `internal` / `private protected` virtual event is not generated. Intermediate concrete event overrides hide the ancestor slot (one override is emitted for the most-derived overridable event). Intermediate `new` / `new virtual` / ordinary same-name event hiders occupy the signature so a misleading `override` is not emitted for a distinct ancestor abstract/virtual slot (same spirit as `#156` for `implement_abstract`; `new virtual` remains overridable itself). `replaceExisting: true` also replaces already-overridden events on this type (match by name), including field-like `override event` declarations (same event-field / selected-declarator removal as `implement_abstract` / `implement_interface`). `new` hiders, explicit interface event implementations, and non-override ordinary events are never replaced. Two same-name existing overrides with no exact match fail with `OverrideExists` (3152) before any write. Methods, properties, and indexers stay unchanged. `callBase` does not apply to events — empty add/remove regardless of `callBase` (abstract and virtual). `members` matches events by name; a name that is not an overridable base member / existing override still fails as today (`OverrideTargetNotFound` 2018). Preview describes generate vs replace for events and writes nothing. Additive with `replaceExisting`, `members`, `callBase`, and `preview`.
- `implement_abstract` now generates missing inherited abstract **events** on the default path: `MemberAnalyzer.GetUnimplementedAbstractMembers` includes `IEventSymbol` in the declared-set filter and the discovery switch (match by name, same as ordinary properties). A type whose only unimplemented abstract members are events succeeds (no `NoUnimplementedAbstractMembers`). Intermediate concrete event overrides still hide the inherited abstract event (same `isConcreteOverride` skip as methods/properties). Intermediate `new` / `new virtual` / ordinary same-name event hiders also occupy the signature so an `override` is not emitted for the ancestor abstract slot (that override would bind the hider and leave the abstract slot unimplemented). `new` hiders and explicit interface implementations still do not count as abstract implementations. Methods, properties, and indexers stay unchanged. `throwNotImplemented` still does not apply to events (empty add/remove via today's `CreateEventStub`). `replaceExisting` still replaces already-implemented abstract events. `members` matches events by name; a name that is not an inherited abstract member still fails as today. Preview describes the generated event and writes nothing. Additive with `replaceExisting`, `members`, and `preview`. `callBase` on `implement_abstract` remains out of scope.
- `implement_abstract` now honors `replaceExisting`: omitted / false keeps today's only-unimplemented members via `MemberAnalyzer.GetUnimplementedAbstractMembers` (plus the `IsImplementableAbstractMember` filter); when every selected member is already implemented the request still fails with `NoUnimplementedAbstractMembers` (3126) and writes nothing. `true` also treats already-implemented abstract members declared on this type as eligible (override methods / properties / indexers that implement an inherited abstract member; intermediate concrete overrides are not treated as implement_abstract targets). Omitted / empty `members` generates every missing abstract member **and** replaces every existing implementable abstract member this tool would emit. A non-empty `members` list is only those names — a name that is not an inherited abstract member still fails as today (`NoUnimplementedAbstractMembers` when the filtered set is empty). Each selected member that already has an implementation on this type is removed (including across every partial that holds it) and a standard generated stub is inserted via today's `CreateMethodStub` / `CreatePropertyStub` / `CreateIndexerStub` paths (events use an override stub with empty add/remove accessors, same body shape as `SyntaxGenerationHelper.CreateEventStub`). Match methods by name + parameter types in order + `RefKind`, properties and events by name, and indexers by the same identity forms as `MatchesRequestedMember` (metadata `Item`, `this[]`, display forms) plus parameter types and `RefKind`. Two same-name existing implementations with no exact signature match fail with `NameCollision` (3003) before any write — the flag does not guess an overload. `new` hiders, explicit interface implementations, and non-override ordinary members are never replaced. Generated stubs still honor `throwNotImplemented` (and indexer / ref-return rules). Additive with `throwNotImplemented`, `members`, and `preview`. Preview describes generate vs replace and writes nothing; when an existing member lives in another partial, preview also includes a Modify pending change for that file (implement_interface / generate_property style). Removal uses `KeepExteriorTrivia` / `KeepDirectives` so a leading `#if` / `#region` on the old member does not orphan a following `#endif` / `#endregion`. Default remains `false` so existing callers keep fail-on-already-implemented. `callBase` on `implement_abstract` is unchanged (out of scope).
- `implement_interface` now honors `replaceExisting`: omitted / false keeps today's only-unimplemented members via `MemberAnalyzer.GetUnimplementedMembers` (plus the `IsImplementableInterfaceMember` filter that skips property/event accessors); when every selected member is already implemented the request still fails with `MemberAlreadyImplemented` (3061) and writes nothing. `true` also treats already-implemented interface members declared on this type as eligible. Explicit replacements require `ExplicitInterfaceImplementations` to contain the selected interface member so `void IA.M()` is not confused with `void IB.M()`. Method type-parameter matching recurses through constructed named types / arrays / pointers / tuples / nullable wrappers so `M<T>(List<T>)` replaces instead of duplicating. Multi-variable event fields (`event Action Changed, Unrelated`) drop only the selected declarator. Omitted / empty `members` generates every missing member **and** replaces every existing implementable member this tool would emit for that interface (ordinary methods, properties, indexers, events). A non-empty `members` list is only those names — a name that is not on the interface still fails as today (`MemberAlreadyImplemented` when the filtered set is empty). Each selected member that already has an implementation on this type is removed (including across every partial that holds it) and a standard generated stub is inserted via today's `CreateMethodStub` / `CreatePropertyStub` / `CreateIndexerStub` / `CreateEventStub` paths. Match methods by name + parameter types in order + `RefKind`, properties and events by name, and indexers by the same identity forms as `MatchesRequestedMember` (metadata `Item`, `this[]`, display forms) plus parameter types and `RefKind`. When `explicitImplementation` is true, replace/match the explicit interface form; when false, the ordinary public stub. Two same-name existing implementations with no exact signature match fail with `NameCollision` (3003) before any write — the flag does not guess an overload. Generated stubs still honor `explicitImplementation` and `throwNotImplemented` (and indexer / ref-return rules). Property/event accessors (`get_Item` / `set_Item` / `add_` / `remove_`) are never emitted as ordinary methods. Additive with `explicitImplementation`, `throwNotImplemented`, `members`, and `preview`. Preview describes generate vs replace and writes nothing; when an existing member lives in another partial, preview also includes a Modify pending change for that file (generate_property / constructor style). Removal uses `KeepExteriorTrivia` / `KeepDirectives` so a leading `#if` / `#region` on the old member does not orphan a following `#endif` / `#endregion`. Default remains `false` so existing callers keep fail-on-already-implemented. `implement_abstract` `replaceExisting` and `callBase` on `implement_interface` are unchanged.
- `implement_interface` now routes missing interface **indexers** (`IPropertySymbol.IsIndexer`) through `SyntaxGenerationHelper.CreateIndexerStub` so they emit `this[...]` indexer declarations (bracketed parameter list) instead of a property named `this[]`. Ordinary properties stay on `CreatePropertyStub`. Methods and events stay on `CreateMethodStub` / `CreateEventStub`. `callBase` stays the default `false` (interface members have no `base` to call). Implicit interface stubs are `public` without `override` (interface members are abstract, but `override` is CS0115 — the shared helper skips `override` when the member's containing type is an interface; abstract-class / virtual / override members used by `implement_abstract` / `generate_overrides` still get `override`). `ref` / `ref readonly` interface indexers keep that return kind; getters still throw when `throwNotImplemented` is false (`return default` is not a valid ref return). Accessors still honor `throwNotImplemented` for ordinary indexers (true → throw bodies; false → default-return getter / empty setter), including explicit implementations. `explicitImplementation`, `members`, and `preview` stay additive; `members` still matches indexer metadata name (`Item`), Roslyn name (`this[]`), and conventional display (`this[int i]`). Preview lists members and writes nothing. Accessor methods (`get_Item` / `set_Item`) are not emitted as ordinary methods.
- `generate_overrides` now honors `callBase` on **properties and indexers**: omitted / true (the existing default) emits `return base.Prop;` / `base.Prop = value;` for non-abstract virtual/override properties, and `return base[i];` / `base[i] = value;` for indexers (same parameter names / `RefKind`); abstract properties still `throw new NotImplementedException();` in accessors (no legal `base` implementation). `false` keeps today's property/indexer stub bodies (default-return getter / empty setter for virtuals). Ordinary method `callBase` is unchanged (`base.M(...)` when true and non-abstract). Additive with `replaceExisting`, `members`, and `preview`. Preview describes that property accessors will (or will not) call `base` (a mixed abstract + virtual selection with `callBase` true qualifies as non-abstract accessors only), and writes nothing. Default remains `true`. `implement_interface` / `implement_abstract` / other generate_* tools are unchanged.
- `generate_method_stub` now honors `replaceExisting`: omitted / false keeps today's fail-on-clash (`NameCollision` 3003) when a compatible ordinary method already exists on the target type (same name, type-parameter arity, parameter count, and parameter types in order via `ValidateNoCompatibleMethod` / `ParameterTypeMatches`); `true` removes that method declaration (including across every partial that holds it) and inserts a freshly generated stub. Body still follows `throwNotImplemented`, `generateAsync`, visibility, and return type. `RefKind` is also matched when the stub can infer it so `ref` / `out` / `in` overloads are not wrongly replaced. Constructors, operators, local functions, explicit interface implementations, accessors, and other non-ordinary methods are never replaced. Two compatible ordinary methods with no single exact target fail with `NameCollision` before any write — the flag does not guess an overload. When no compatible method exists, generation proceeds as today. Additive with `methodName`, `returnType`, `visibility`, `generateAsync`, `throwNotImplemented`, and `preview`. Default remains `false` so existing callers keep fail-on-clash. Preview describes generate vs replace and writes nothing; when the existing method lives in another partial, preview also includes a Modify pending change for that file (generate_property-style). Removal uses `KeepExteriorTrivia` / `KeepDirectives` so a leading `#if` / `#region` on the old method does not orphan a following `#endif` / `#endregion`. Other generate_* / implement_* tools are unchanged.
- `generate_method_stub` now honors `throwNotImplemented`: omitted / true keeps today's `throw new global::System.NotImplementedException();` stub; `false` uses `SyntaxGenerationHelper.CreateDefaultReturnBody` (empty block for `void`; `return null;` for reference types; `return default(T);` for value types / type parameters). Async `Task` / `Task<T>` stubs (from `generateAsync` or an awaited call site) unwrap the task type so a non-throwing body still compiles — empty block for `async Task`, default-return of the inner type for `async Task<T>` (never `return null;` inside `async Task`; no dummy storage). `ref` / `ref readonly` returns still throw even when the flag is false (`return default` is not a valid ref return). `methodName`, `returnType`, `visibility`, `generateAsync`, and `preview` stay additive. Preview describes throwing vs non-throwing and writes nothing. Default remains `true` so existing callers keep throw stubs. `implement_interface` and `implement_abstract` are unchanged.
- `generate_property` now honors `replaceExisting`: omitted / false keeps today's fail-on-clash (`NameCollision` 3003) when any non-implicit member with the target property name already exists on the type; `true` removes the existing property declaration(s) that match that name (including across every partial that holds one) and inserts a freshly generated property. Auto / init-only / backing-field form still follows `propertyName`, `propertyType`, `fieldName`, `visibility`, and `initOnly`. Fields and methods of the same name are never removed and still fail with `NameCollision`. Two same-named properties (for example a public property plus an explicit interface property) fail with `NameCollision` before any write — the flag does not guess. Name matching scans all members (not only `GetMembers(name)`) so explicit-interface properties whose metadata name is `IFoo.Name` still count. Record / primary-constructor properties with no `PropertyDeclarationSyntax` are not replaced (`NameCollision`, same spirit as constructor never replacing a primary constructor). When no matching property exists, generation proceeds as today. Additive with existing params. Default remains `false` so existing callers keep fail-on-clash. Preview describes generate vs replace and writes nothing; when the existing property lives in another partial, preview also includes a Modify pending change for that file (constructor-style). Removal uses `KeepExteriorTrivia` / `KeepDirectives` so a leading `#if` / `#region` on the old property does not orphan a following `#endif` / `#endregion`.
- `implement_abstract` now honors `throwNotImplemented`: omitted / true keeps today's `throw new global::System.NotImplementedException();` stubs for methods, property accessors, and indexer accessors; `false` uses `SyntaxGenerationHelper.CreateDefaultReturnBody` for methods and getters (empty block for `void`; `return null;` / `return default(T);` otherwise) and empty blocks for setters / init setters — same body shape as `implement_interface` when throw is false. `ref` / `ref readonly` methods and getters still throw even when the flag is false (`return default` is not a valid ref return; no dummy storage is invented). Override modifiers, accessibility, required, init-only, type parameters, `members`, and `preview` stay unchanged. Preview describes throwing vs non-throwing stubs and writes nothing. Default remains `true` so existing callers keep throw stubs. `implement_interface` is unchanged.
- `generate_tostring` now honors `callSuper`: omitted / false keeps today's member-only ToString (no `base.ToString()`); `true` folds the immediate base type's parameterless instance `ToString` into the generated override — `{base.ToString()}` first in an interpolated body (before this type's member interpolations), and `sb.Append(base.ToString())` first on the StringBuilder path (before this type's member Appends). Rejected when the immediate base is `System.Object` or `System.ValueType` (`CallSuperOnObjectBase` 3146), or when the immediate base's parameterless instance `ToString()` is abstract (`CallSuperOnAbstractBase` 3147). Zero collected members is allowed when `callSuper` is true (base-only ToString; skips `NoMembersToGenerate` 3055). Distinct from `includeInheritedMembers` (base-method fold-in vs member collection); combining both is allowed. Additive with `fields`, `includeProperties`, `format`, `includeInheritedMembers`, `replaceExisting`, and `preview`. Preview describes that base ToString will be included and writes nothing. `generate_equals_hashcode` `callSuper` is unchanged.
- `generate_overrides` now honors `replaceExisting`: omitted / false keeps today's skip of members this type already overrides (`GetOverridableMembers` plus Object `ToString` / `Equals(object)` / `GetHashCode`); a named member that is already overridden still fails with `OverrideTargetNotFound` (2018) before any write. `true` makes those already-overridden members eligible alongside missing overridable members. Omitted / empty `members` generates missing overrides **and** replaces every existing override this tool would have emitted (overridable base virtual/abstract methods and properties, plus Object `ToString` / `Equals(object)` / `GetHashCode`). A non-empty `members` list is only those names — a name that is neither an overridable base member nor an existing override still fails with `OverrideTargetNotFound` before any write. Each selected member that already has an override on this type is removed (method or property, including across every partial that holds it) and a standard generated override is inserted. Match methods by name + parameter types in order + `RefKind`, and properties by name. Two existing overrides that share a name with no exact signature match fail with `OverrideExists` (3152) before any write — the flag does not guess an overload. Generated bodies still honor `callBase` (`true` / default emits `base.M(...)` for non-abstract virtuals; abstract members still `throw new NotImplementedException();`; `false` keeps today's no-`base` stub). `new` hiders, explicit interface implementations, non-override methods of the same name, and primary constructors are never replaced. Extra modifiers on the old override (`sealed`, `async`, attributes) are not copied. The regenerated override keeps the inherited accessibility from the base target (`protected` / `protected internal` / `private protected` / `internal`, not a hardcoded `public` — CS0507) and each parameter's `RefKind` on the declaration and on `base.M(...)` (`ref` / `out` / `in` / `ref readonly`). Method type parameters match by ordinal so `Base.M<T>(T)` replaces `Derived.M<T>(T)` instead of appending a duplicate or hitting `OverrideExists`. Additive with `callBase`, `members`, and `preview`. Preview describes which overrides will be generated vs replaced, and writes nothing.
- `generate_constructor` `callBase` now also applies to **record classes** (non-copy): omitted / false keeps today's record non-copy shape (no `: base(...)`). Record copy constructors (`: base(other)`), `classBaseCopy`, and ordinary-class `callBase` stay unchanged. `true` with `copyConstructor` is still rejected with `CallBaseConflictsWithCopyConstructor` (3149) before any write. When `true` without `copyConstructor` and the target is a record class (not a record struct / struct / ordinary class) whose immediate base is a class or record other than `object` / `ValueType`, the same prefix-matching rules as ordinary-class `callBase` apply: accessible instance constructors (public / protected / protected-internal, plus internal / private-protected when the same assembly) whose parameter types in order (by-value, same `RefKind`) are a prefix of the generated constructor are candidates; longest prefix wins, then case-insensitive parameter-name matches; a remaining tie is `AmbiguousBaseConstructor` (3151). A match of length `k >= 1` emits `: base(p1, ..., pk)` and does not reassign those members; only an accessible parameterless match emits `: base()` without skipping body members; `addNullChecks` still guards forwarded non-nullable reference / `Nullable<T>` parameters. No accessible match and no accessible parameterless constructor is `NoMatchingBaseConstructor` (3150) (preview also rejects). `object` / `ValueType` / struct bases emit no `: base(...)` and do not fail. `includeInheritedMembers` orders inherited members first only when the record is eligible (ignored `callBase` stays order-neutral). Record structs and structs still ignore the flag (including member order). Additive with `visibility`, `replaceExisting`, `includeProperties`, `includeInheritedMembers`, `members`, `addNullChecks`, `classBaseCopy`, and `preview`. Preview describes whether a `: base(...)` initializer was added (and which base constructor), and writes nothing. Primary constructors are still never replaced.
- `generate_constructor` now honors `callBase`: omitted / false keeps today's ordinary-class non-copy shape (no `: base(...)`). Record copy constructors and `classBaseCopy` stay unchanged. `true` with `copyConstructor` is rejected with `CallBaseConflictsWithCopyConstructor` (3149) before any generate, replace, or preview write. When `true` without `copyConstructor` and the target is an ordinary class (not a record / struct / record struct) whose immediate base is a class other than `object`, accessible instance constructors on that base (public / protected / protected-internal, plus internal / private-protected when the same assembly) are candidates: a base constructor whose parameter types in order (by-value, same `RefKind`) are a prefix of the generated constructor's parameter types is selected, preferring the longest prefix, then case-insensitive parameter-name matches; a remaining tie is rejected with `AmbiguousBaseConstructor` (3151) before any write. A match of length `k >= 1` emits `: base(p1, ..., pk)` using the generated constructor's first `k` parameter identifiers and does not reassign the members that produced those parameters; `addNullChecks` still emits `ArgumentNullException` guards for those forwarded parameters in the derived body before assignments. An accessible parameterless base constructor as the only usable match emits `: base()` without skipping body members. No accessible match and no accessible parameterless constructor is rejected with `NoMatchingBaseConstructor` (3150) (preview also rejects). When the base is `object` / a struct / a record, no `: base(...)` is emitted and generation still succeeds. Records, record structs, and structs ignore the flag (including member order). Additive with `visibility`, `replaceExisting`, `includeProperties`, `includeInheritedMembers`, `members`, `addNullChecks`, `classBaseCopy`, and `preview`. Preview describes whether a `: base(...)` initializer was added (and which base constructor), and writes nothing.
- `generate_constructor` now honors `classBaseCopy`: omitted / false keeps today's class copy-constructor shape (no `: base(...)` on ordinary classes). Record copy constructors stay unchanged (they already chain). `true` requires `copyConstructor` — otherwise the request is rejected with `ClassBaseCopyRequiresCopyConstructor` (3148) before any generate, replace, or preview write. When both flags are true and the target is an ordinary class (not a record / struct / record struct) whose immediate base is a class other than `object` with an accessible instance constructor of exactly one by-value parameter of the **base** type, the generated copy constructor emits `: base((Base)<copyParameter>)` (argument cast to the immediate base type) so `Derived(Derived other) : base((Base)other)` binds to that `Base(Base)` constructor even when `Base(IFoo)` or a more-specific inaccessible `Base(Derived)` would otherwise win. Members the base copy constructor already copies (inherited assignable members) are not reassigned in the derived body, including when `includeInheritedMembers` is true; this-type members are still assigned as today. Public / protected / protected-internal base copy constructors count, plus internal / private-protected when the same assembly; private / otherwise-inaccessible constructors do not. When the base is `object` / a struct / a record, or no accessible base copy constructor exists, no class `: base(...)` is emitted and generation still succeeds. Records, record structs, and structs ignore the flag. Additive with `visibility`, `replaceExisting`, `includeProperties`, `includeInheritedMembers`, `members`, `addNullChecks`, and `preview`. Preview describes whether a class `: base(<copyParameter>)` initializer was added, and writes nothing.
- `generate_constructor` now honors `copyConstructor`: omitted / false keeps today's one-parameter-per-member constructor; `true` generates a single-parameter constructor whose parameter type is the target type and whose body assigns each selected member from that parameter (`this.Name = other.Name;`). Parameter name prefers `other`, then `source`, then `original` — the first identifier that does not collide with an existing member or type parameter on the type. Member selection still uses `members` / `includeProperties` / `includeInheritedMembers` (inherited readonly fields stay skipped). In copy mode, auto-collection skips properties without an accessible getter (`other.Member` must compile); an explicit named member that cannot be read fails with `MemberNotFound`. `addNullChecks` null-checks the single copy parameter on reference types (class / record class) and is skipped on structs / record structs. `ConstructorExists` / `replaceExisting` treat the copy-constructor signature as exactly one by-value parameter of the target type; `replaceExisting` may replace an exact match; optional/required ambiguity without an exact match still rejects; primary constructors are still never replaced. `visibility` still applies, including struct protected `InvalidVisibility` rejects. Unsealed records reject copy-constructor visibilities other than `public` / `protected` with `InvalidVisibility` (1014, CS8878) before any generate, replace, or preview write; sealed records and record structs may keep other valid visibilities. Derived records whose base is also a record emit `: base(<copyParameter>)` (CS8868); ordinary classes do not (class base-copy remains out of scope). Additive with `includeProperties`, `includeInheritedMembers`, `members`, `addNullChecks`, `replaceExisting`, `visibility`, and `preview`. Preview describes copy-constructor mode, the chosen parameter name, and members, and writes nothing.
- `generate_constructor` now honors `visibility`: omitted / null / empty / `public` keeps today's public constructor; `private`, `protected`, `internal`, `protected internal`, and `private protected` emit that accessibility on the generated constructor declaration. Invalid values fail with `InvalidVisibility` (1014) and write nothing. Structs and record structs reject `protected` / `protected internal` / `private protected` with `InvalidVisibility` (1014, CS0666) before any generate, replace, or preview write; `public` / `internal` / `private` / omitted stay valid on structs. Classes and record classes keep all six visibilities. When `replaceExisting` is true and an exact-signature constructor is replaced, the **new** constructor uses the requested visibility (the old constructor's accessibility is not copied). Additive with `includeProperties`, `includeInheritedMembers`, `members`, `addNullChecks`, `replaceExisting`, and `preview`. Preview mentions the chosen visibility and writes nothing. `MemberNotFound`, `ConstructorExists`, primary-ctor refuse, and RefKind exact-match stay unchanged. No copy-constructor options in this increment.
- `generate_constructor` now honors `replaceExisting`: omitted / false keeps today's `ConstructorExists` rejects (exact signature match, optional-param ambiguity, required-param conflict); `true` removes an existing non-implicit constructor with the **exact** same signature (same parameter count, types in order via `SymbolEqualityComparer`, and `RefKind` — generated parameters are by-value, so `ref`/`out`/`in` overloads are left alone) across every partial that holds it, then inserts the newly generated constructor for the requested `includeProperties` / `includeInheritedMembers` / `members` / `addNullChecks`. Optional-param / required-param **ambiguity** without an exact match still throws `ConstructorExists` — the flag does not guess which overload to replace. Class/record **primary constructors** are never replaced (`ConstructorExists` even when the flag is true) because their declaring syntax is the type parameter list, not a constructor declaration. When no exact-signature constructor exists, generation proceeds as today (no remove). Implicit constructors are not treated as existing (already today's rule). Additive with `includeProperties`, `includeInheritedMembers`, `members`, `addNullChecks`, and `preview`. Preview with an exact match describes replacement and writes nothing; when the constructor lives in another partial, preview also includes a Modify pending change for that file with the existing constructor as `BeforeSnippet`. `MemberNotFound` is unchanged. No visibility or copy-constructor options in this increment.
- `generate_constructor` now honors `includeInheritedMembers`: omitted / false keeps today's this-type-only collection in `GetMembersToInitialize`; `true` also walks `BaseType` until `System.Object` / `System.ValueType` and appends accessible instance fields (and, when `includeProperties` is true, settable properties) declared on those bases — public / protected / protected-internal, plus internal when the same assembly; private and otherwise-inaccessible members are skipped. Collection stays in `GetMembersToInitialize` (constructor settable-property filter: writable/settable, not the readable-property `EqualityMemberCollector` path). Writable indexers (`IsIndexer`) are never collected on this-type or inherited paths — a constructor cannot supply index arguments. Inherited readonly fields are skipped because a derived constructor cannot assign them (CS0191). Hide/override skip matches equals/tostring: any closer non-implicit member with the same name (including methods and nested types) hides the inherited one. This-type members keep today's relative order (immediate base first, then further bases). A non-empty `members` list stays authoritative and, when the flag is true, resolves listed names against this type and accessible inherited members — an explicit property name is still initialized even if `includeProperties` is false. Additive with `includeProperties`, `members`, `addNullChecks`, and `preview`. Preview describes inherited inclusion and writes nothing. `MemberNotFound` and `ConstructorExists` are unchanged. No `replaceExisting`, visibility, or copy-constructor options in this increment.
- `generate_constructor` now honors `includeProperties`: omitted / true keeps today's field+settable-property collection (non-static, non-const, non-implicit fields; non-static settable non-readonly non-implicit properties); `false` with no `members` list collects instance fields only. A non-empty `members` list is authoritative and still resolves listed names against fields and settable properties (an explicit property name is still initialized). Auto-property backing fields stay excluded (`IsImplicitlyDeclared`). Additive with `members`, `addNullChecks`, and `preview`. Preview describes the chosen members and writes nothing. No `includeInheritedMembers`, visibility, or copy-constructor options in this increment.
- `generate_tostring` now honors `includeProperties`: omitted / true keeps today's field+property collection (instance fields plus readable non-indexer properties; still skip static/const/implicit/indexers); `false` with no `fields` list collects instance fields only. A non-empty `fields` list is authoritative and still resolves listed names against fields and properties (an explicit property name is still included in ToString). Auto-property backing fields stay excluded (`IsImplicitlyDeclared`). Members named `ToString` are still dropped after collection so the generated override cannot hide `this.ToString`. Additive with `fields`, `format`, `includeInheritedMembers`, and `replaceExisting`. Preview describes the chosen members / format and writes nothing. `generate_equals_hashcode` is unchanged (already honors the flag). Collection always uses the four-parameter `EqualityMemberCollector.CollectMembers` overload so the flag is honored on both this-type-only and inherited paths (the two-parameter always-true forward is no longer used).
- `generate_tostring` now honors `replaceExisting`: omitted / false keeps today's fail-on-existing behavior (`AlreadyHasOverride` 3056, "Type already has a ToString override.") when a non-implicit non-generic parameterless `ToString` already exists (instance or static); `true` removes those parameterless `ToString()` members on the target type across every partial that holds one (instance and static, so a leftover `static ToString()` cannot collide with the generated override / CS0111), then generates a fresh instance override for the requested `fields` / `format` / `includeInheritedMembers`. Generic `ToString<T>()` (arity ≠ 0) and parameterized overloads (`ToString(string)`, `ToString(IFormatProvider)`, etc.) are left alone and do not trip 3056. Implicit `Object` / `ValueType` `ToString` does not count as existing (already today's rule). Additive with `fields`, `format`, and `includeInheritedMembers`. Preview describes the replacement / generated members and writes nothing. No new error codes for the happy replace path.
- `generate_tostring` now honors `includeInheritedMembers`: omitted / false keeps today's this-type-only collection (two-parameter `EqualityMemberCollector.CollectMembers`); `true` also collects accessible inherited instance fields and readable properties via the four-parameter overload (`includeProperties: true`, `includeInheritedMembers: true`). Accessibility / Object-ValueType stop / static-const-implicit-indexer filters / hide-override skip / order come from the existing collector. Hide now treats any closer non-implicit member with the same name as a hider (methods and nested types included, not only fields/properties/events) so generated `this.Name` cannot bind to a method group (CS0119). Members named `ToString` are dropped so the generated override cannot hide `this.ToString`. A non-empty `fields` list stays authoritative and, when the flag is true, resolves listed names against this type and accessible inherited members. Additive with `fields` and `format`. Preview describes the chosen members / format and writes nothing. `generate_equals_hashcode` is unchanged (already honors the flag). The existing two- and three-parameter `CollectMembers` overloads stay this-type-only.
- `generate_equals_hashcode` now honors `includeInheritedMembers`: omitted / false keeps today's this-type-only collection (`typeSymbol.GetMembers()`); `true` also walks `BaseType` until `System.Object` / `System.ValueType` and appends accessible instance fields (and, when `includeProperties` is true, readable properties) declared on those bases — public / protected / protected-internal, plus internal when the same assembly; private and otherwise-inaccessible members are skipped. This-type members keep today's relative order (immediate base first, then further bases). A non-empty `fields` list stays authoritative and, when the flag is true, resolves listed names against this type and accessible inherited members. Distinct from `callSuper` (member collection vs base-method fold-in); combining both is allowed. Additive with `fields`, `includeProperties`, `implementIEquatable`, `generateOperators`, `replaceExisting`, `useHashCodeCombine`, and `callSuper`. Preview describes inherited inclusion and writes nothing. `generate_tostring` is unchanged (still this-type collection). The existing two- and three-parameter `EqualityMemberCollector.CollectMembers` overloads are preserved and forward `includeInheritedMembers: false` so NuGet callers compiled against the previous package do not hit `MissingMethodException`.
- `generate_equals_hashcode` now honors `callSuper`: omitted / false keeps today's member-only Equals/GetHashCode (no `base.Equals` / `base.GetHashCode`); `true` folds the immediate base type's equality into both methods — `base.Equals(other)` before member comparisons (object Equals after `obj is T other`; typed Equals when `implementIEquatable` binds the best applicable base Equals), and `base.GetHashCode()` as the first `HashCode.Combine` / builder `Add` argument or as the prime-multiply seed instead of `17`. Rejected when the immediate base is `System.Object` or `System.ValueType` (`CallSuperOnObjectBase` 3146), or when the immediate base's `Equals(object)` or `GetHashCode` is abstract (`CallSuperOnAbstractBase` 3147). Zero collected members is allowed when `callSuper` is true (base-only methods; skips `NoMembersToGenerate` 3055). Additive with `fields`, `includeProperties`, `implementIEquatable`, `generateOperators`, `replaceExisting`, and `useHashCodeCombine`. Preview describes base inclusion and writes nothing.
- `generate_equals_hashcode` now honors `includeProperties`: omitted / true keeps today's field+property collection (instance fields plus readable non-indexer properties; still skip static/const/implicit/indexers); `false` with no `fields` list collects instance fields only. A non-empty `fields` list is authoritative and still resolves listed names against fields and properties (an explicit property name is still hashed/equaled). Auto-property backing fields stay excluded (`IsImplicitlyDeclared`). Additive with `fields`, `implementIEquatable`, `generateOperators`, `replaceExisting`, and `useHashCodeCombine`. Preview describes the chosen members and writes nothing. `generate_tostring` is unchanged (still includes properties by default). The existing two-parameter `EqualityMemberCollector.CollectMembers` overload is preserved and forwards with `includeProperties: true` so NuGet callers compiled against the previous package do not hit `MissingMethodException`.
- `generate_equals_hashcode` now honors `useHashCodeCombine`: omitted / true keeps today's `HashCode.Combine` (≤8 members) or `HashCode` builder (&gt;8 members), qualifying `global::System.HashCode` so a type-local `HashCode` cannot steal the call; `false` emits a classic unchecked prime-multiply `GetHashCode` (seed 17, `hash = (hash * 31) + memberHash`) with `this.Member.GetHashCode()` for non-nullable members and `(this.Member?.GetHashCode() ?? 0)` for nullable reference / nullable value members. Combine / builder / prime-multiply member accesses are qualified with `this.` so a field named `hash` is not shadowed by the accumulator local. Additive with `fields`, `implementIEquatable`, `generateOperators`, and `replaceExisting`. Preview describes the chosen GetHashCode style and writes nothing.
- `generate_equals_hashcode` now honors `replaceExisting`: omitted / false keeps today's fail-on-existing behavior (`AlreadyHasOverride` 3056, `AlreadyImplementsIEquatable` 3144, `AlreadyHasEqualityOperators` 3145); `true` removes existing `Equals(object)` and `GetHashCode()` before generating, and also removes typed `Equals(T)` / `System.IEquatable<T>` when `implementIEquatable` is true, and `operator ==` / `operator !=` when `generateOperators` is true. Existing operators are left alone when `generateOperators` is false. Preview writes nothing.
- `generate_equals_hashcode` now honors `generateOperators`: omitted / false keeps today's no-operator behavior; `true` adds `public static bool operator ==` and `operator !=` that agree with the generated Equals (`global::System.Object.Equals(left, right)` / `!(left == right)`). Class parameters are nullable (`Person?`); struct parameters are not (`Point`). Generic types use the constructed self type (`Box<T>`). Additive with `implementIEquatable`. Types that already declare `==` or `!=` fail with AlreadyHasEqualityOperators (3145). Preview writes nothing.
- `generate_equals_hashcode` now honors `implementIEquatable`: omitted / false keeps today's Equals(object)+GetHashCode-only behavior; `true` adds `global::System.IEquatable<{TypeName}>`, a typed `Equals(T)` (nullable for class, not for struct) comparing the same members, and has `Equals(object)` delegate to it. Types that already implement IEquatable&lt;T&gt; or already have a compatible typed Equals fail with AlreadyImplementsIEquatable (3144). Preview writes nothing.
- `generate_tostring` now honors `format`: omitted / null / empty / `"interpolated"` (case-insensitive) keeps today's interpolated-string `ToString`; `"stringbuilder"` (case-insensitive) builds the same `TypeName { Field = value, ... }` text via `global::System.Text.StringBuilder`, qualifying member accesses with `this.` so a field named `sb` is not shadowed by the local builder. Unknown values fail with InvalidToStringFormat (3143). Preview writes nothing.
- `extract_base_class` now honors `separateFile`: default `false` keeps today's same-file extract unless `targetFile` is set; `true` with no `targetFile` writes `{BaseClassName}.cs` next to the source; explicit `targetFile` always wins; rejects an existing sibling with TargetFileExists (3019). Preview writes nothing. When default compile items are disabled, the new sibling is added as an explicit `Compile Include`. Nested types reject a separate-file extract (3142).
- `extract_interface` now honors `separateFile`: default `false` keeps today's same-file extract unless `targetFile` is set; `true` with no `targetFile` writes `{InterfaceName}.cs` next to the source; explicit `targetFile` always wins; rejects an existing sibling with TargetFileExists (3019). Preview writes nothing.
- `sort_usings` now honors `systemFirst`: default `true` keeps today's System / System.* first grouping within regular and static usings; `false` keeps group order (regular, static, alias) but sorts namespaces alphabetically with no System priority. Alias usings stay alphabetical by alias. Global usings stay ahead of non-global usings in both modes (CS8915). Preview writes nothing.
- `move_type_to_namespace` now honors `updateFileLocation`: when true, moves the source file to a folder matching the target namespace (e.g. `MyApp.Services` → `MyApp/Services/`), creating missing directories and updating project document paths / explicit compile items; preview writes nothing (including folders); rejects destination-exists, file-name collision, uneditable project, and missing file. Default `false` keeps today's namespace-only rewrite.

### Added
- `simplify_name` — remove redundant namespace qualifications from type (and similar) references when a using directive or the current namespace already makes the short name bind to the same symbol (UC-A8). `scope` is `file` (default; every eligible qualified name in the file) or `location` (the qualified name at `line` / optional `column`). Preview describes the simplifications and writes nothing. Names that would become ambiguous or bind to a different symbol are skipped and reported (`simplificationsSkipped` / `skippedReasons`); `global::` aliases are left alone unless they are truly redundant with the same binding. Names inside `nameof` / attributes are simplified only when the semantic model confirms the meaning is unchanged. Rejects no-simplifiable-names (3159) for location scope with no simplifiable name at the location and for file scope with nothing to simplify. `line` is required for location scope (1006). Spec listed 3123; that code already ships as `NoValidInstanceReceiver`, so this leftover allocates 3159.
- `remove_braces` — remove braces from control statements (`if`, `else`, `for`, `foreach`, `while`, `using`) whose body is a single-statement block, preserving semantics (UC-A6). `scope` is `statement` (default; unwrap the braced body at `line` / optional `column`), `file` (every eligible single-statement braced control body in the file), or `type` (bodies inside `typeName`). `else if` is left as a single construct in file/type scope (the inner if body is unwrapped; an `else { if }` block is not collapsed into `else if`). Statement scope targeting the `else` keyword may unwrap `else { if }` to `else if`. Preview describes the unwrapped statements and writes nothing. Rejects no-control-statement (3155), no-braces-to-remove (3157), and multiple-statements-in-block (3158) for statement scope. `line` is required for statement scope (1006); `typeName` is required for type scope (1005). Statement scope rejects when unwrapping would create a dangling else, produce a non-embeddable statement (CS1023: local declaration, local function, or labeled body), or change a label that an external `goto` needs (4004); file/type skip those bodies. Non-whitespace trivia on the removed `{` / `}` is kept on the surviving statement. Type scope resolves namespace-qualified names (`A.C`) and rejects an ambiguous simple name (2004) instead of editing the first match.
- `add_braces` — add braces to control statements (`if`, `else`, `for`, `foreach`, `while`, `using`) that have a single-statement body, preserving semantics (UC-A5). `scope` is `statement` (default; wrap the braceless body at `line` / optional `column`), `file` (every braceless control body in the file), or `type` (bodies inside `typeName`). `else if` is left as a single construct in file/type scope (the inner if body is wrapped). Preview describes the wrapped statements and writes nothing. Rejects no-control-statement (3155) and already-has-braces (3156) for statement scope. `line` is required for statement scope (1006); `typeName` is required for type scope (1005). Statement scope rejects when wrapping would hide a label from an external `goto` (4004); file/type skip those bodies. Type scope resolves namespace-qualified names (`A.C`) and rejects an ambiguous simple name (2004) instead of editing the first match.
- `invert_if` — flip an if-statement condition and swap the if/else branches (UC-A4), flipping comparison operators and applying De Morgan to `&&` / `||`. An if without else gets an empty if body and the original body as else. Preview describes the inverted condition / swapped branches and writes nothing. Rejects no-if-at-location (3153) and conditions that cannot be safely inverted (3154, for example a pattern or out variable).
- `pull_members_up` — move selected members from a derived type onto an existing base class or interface, with preview mode and rejects for missing bases, interface-incompatible members, and name/signature conflicts
- `push_members_down` — move selected members from a base type down onto derived types, with optional named targets, preview mode, and rejects for missing derived types, conflicts, interface-incompatible members, and uneditable targets
- `use_base_type` — replace derived-type references with a compatible base type or interface, rewriting only usages whose members exist on that base, with preview mode and rejects for missing bases, uneditable documents, and no eligible references
- `introduce_field` — turn a selected local variable or expression into a class field, optionally initializing it in a constructor, with preview mode and rejects for missing expressions, uneditable documents, and invalid target types
- `safe_delete` — delete a selected symbol only when it has no remaining references, with preview mode and rejects that include usage locations, missing symbols, and uneditable documents
- `make_static` — add the static modifier to a selected instance method that does not use instance state, update call sites and method-group conversions to the containing type name, with preview mode and rejects for instance-member access, already-static methods, and uneditable documents
- `make_non_static` — remove the static modifier from a selected static method, rewrite type-name call sites and method-group conversions to an instance receiver (or `this` in the same type), with preview mode and rejects for already-instance methods, missing receivers, extern methods, and uneditable documents
- `convert_to_block_body` — convert a selected expression-bodied member (`=> expr`) to a block body (`{ return expr; }` or `{ expr; }` as appropriate), including properties and accessors, with preview mode and rejects for missing symbols, already-block bodies, unsupported members, and uneditable documents
- `generate_property` — generate an auto-property `{ get; set; }`, init-only property `{ get; init; }`, or backing-field property `{ get => field; set => field = value; }` on a selected type, with preview mode and rejects for missing symbols, uneditable documents, unsupported targets, and name clashes
- `generate_method_stub` — generate a method from an undefined call site, inferring target type, parameters, and return type from usage, with a `throw new NotImplementedException();` body, preview mode, and rejects for missing call sites, existing methods, uneditable or external targets, and uninferable return types
- `implement_abstract` — generate implementation stubs for unimplemented abstract methods, properties, indexers, and events inherited by a selected class, with a `throw new NotImplementedException();` body (events use empty add/remove), preview mode, and rejects for missing symbols, no unimplemented abstract members, uneditable documents, and unsupported targets
- `inline_constant` — replace references to a `const` field with a formatted literal (typed null casts), optionally remove the declaration, with preview mode and rejects for non-constants (including static readonly), public API constants, attribute usages, missing symbols, and uneditable documents
- `add_parameter` — add a named parameter to a method and update call sites, overrides, and interface implementations, with preview mode and rejects for duplicate names, invalid types, out-of-range positions, required-after-optional, params-not-last, missing methods, and uneditable documents
- `remove_parameter` — remove a named parameter from a method and drop matching call-site arguments, with override/interface updates, preview mode, force-gated body handling that must stay compiling, and rejects for a missing parameter, used-in-body without force, method-group references, missing methods, and uneditable documents
- `reorder_parameters` — reorder a method's parameters by a 0-based permutation and update positional call-site arguments, with override/interface updates, preview mode, and rejects for invalid permutations, params-not-last, optional-before-required, method-group references, missing methods, and uneditable documents
- `change_return_type` — change a method's declared return type and convert return statements when safe (implicit conversions left alone; void↔non-void rewritten), with override/interface updates, preview mode, per-document type qualification, and rejects for same type, invalid type syntax, incompatible returns or invocation result contexts, uneditable override/interface contracts, async/Task/ValueTask, iterators, overload collisions, method-group references that would break, missing methods, and uneditable documents
- `convert_anonymous_to_class` — convert an anonymous type (`new { ... }`) to a named class or record with matching public properties, replace same-shape anonymous creations in the solution, with preview mode and rejects for non-anonymous locations, invalid type names, name conflicts, missing files, and uneditable documents
- `convert_tuple_to_struct` — convert a C# tuple / `ValueTuple` creation to a named struct with matching public members, replace same-shape tuple creations in the solution, with preview mode and rejects for non-tuple locations, invalid type names, name conflicts, missing files, uneditable documents, less-accessible member types, and method type-parameters
- `rename_file_to_match_type` — rename a source file so its name matches the primary type declared in it (preserve directory), without renaming the type, constructors, or references, with preview mode and rejects for already-matching names, no type, multiple types without disambiguation, destination-exists, missing files, and uneditable documents
- `rename_namespace` — rename a namespace across the solution, updating declarations, using directives, and qualified name references; `updateFolders` moves a folder whose path matches the old namespace and updates document paths / explicit compile items (including wildcard patterns), with preview mode and rejects for same name, invalid namespace name, missing namespace, name conflicts, missing files, uneditable documents, destination-folder-exists, nested destination, folder that contains a project file, unsupported compile items, file-name collision, and folder path that does not correspond to the namespace

## [0.4.0] - 2026-02-23

### Added
- `.slnx` (Visual Studio 2022+ XML solution format) support in workspace loading and CLI
- Codex CLI compatibility via JSON-RPC 2.0 notification handling (`notifications/initialized`)
- `TestSolution.slnx` test fixture

### Changed
- Roslyn upgraded from 4.8.0 to 5.0.0 (`Microsoft.CodeAnalysis.CSharp.Workspaces`, `Microsoft.CodeAnalysis.Workspaces.MSBuild`)
- Workspace failure handler migrated to `RegisterWorkspaceFailedHandler` API (Roslyn 5.0)
- xunit upgraded from 2.6.0 to 2.9.3 across all test projects
- Microsoft.NET.Test.Sdk upgraded from 17.8.0 to 18.0.1 across all test projects
- coverlet.collector upgraded from 6.0.0 to 6.0.4 across all test projects
- GitHub Actions: checkout v4 to v6, setup-dotnet v4 to v5, upload-artifact v4 to v6
- 566 total tests (270 Core + 206 Server + 90 Cli)

### Fixed
- RS1039 analyzer warning from Roslyn 5.0 upgrade (suppressed in SymbolKindMapperTests)
- Import ordering in Cli.Tests to pass format check

## [0.3.1] - 2026-02-09

### Fixed
- Republish all packages — NuGet 0.3.0 was immutable from a prior incomplete release

## [0.3.0] - 2026-02-09

### Added
- 22 new tools (41 total), organized across five categories:

  **Code Navigation (5 tools)**
  - `find_references` — find all references to a symbol across the solution
  - `go_to_definition` — navigate to the source definition of a symbol
  - `get_symbol_info` — retrieve detailed metadata for any symbol (type, accessibility, modifiers, members, docs)
  - `find_implementations` — find all implementations of an interface or abstract member
  - `search_symbols` — search for symbols by name pattern across the workspace

  **Analysis & Metrics (6 tools)**
  - `get_diagnostics` — retrieve compiler diagnostics filtered by severity and file
  - `get_code_metrics` — calculate cyclomatic complexity, lines of code, maintainability index, class coupling, and depth of inheritance
  - `analyze_control_flow` — analyze control flow for a code region (reachability, return/exit points)
  - `analyze_data_flow` — analyze data flow for a code region (reads, writes, captured variables)
  - `get_document_outline` — get a hierarchical outline of all symbols in a file
  - `get_type_hierarchy` — retrieve base types and derived classes for a type

  **Code Generation & Formatting (4 tools)**
  - `generate_equals_hashcode` — generate Equals() and GetHashCode() overrides for a type
  - `generate_tostring` — generate ToString() override for a type
  - `format_document` — format a C# file using Roslyn's built-in formatter
  - `add_null_checks` — add null-check statements for method parameters

  **Code Conversions (7 tools)**
  - `convert_expression_body` — toggle between expression body and block body for methods/properties
  - `convert_property` — convert between auto-property and full property with backing field
  - `introduce_parameter` — promote a local variable to a method parameter, updating call sites
  - `convert_foreach_linq` — convert foreach loops with Add patterns to LINQ expressions
  - `convert_to_pattern_matching` — convert if/is chains and switch statements to switch expressions
  - `convert_to_interpolated_string` — convert string.Format() and concatenation to interpolated strings
  - `find_callers` — find all callers of a symbol across the solution

- `roslyn-cli` standalone CLI tool (`RoslynMcp.Cli` package)
  - All 41 Roslyn tools accessible from the command line without an AI assistant
  - JSON output by default (pipeable to `jq`), with `--format text` for human-readable output
  - Per-tool help via `roslyn-cli <tool-name> --help`
  - Exit codes: 0=success, 1=tool error, 2=CLI error, 3=environment error

- `QueryOperationBase<TParams, TResult>` — new base class for read-only query operations
- `SymbolResolver` — general-purpose symbol resolver (position-based and name-based)
- New contract models, error codes, and enums for all 22 new tools
- New shared utilities: `MetricsCalculator`, `EqualityMemberCollector`, `NullCheckGenerator`

### Fixed
- Null-ref in `GetCodeMetrics` when metrics are unavailable
- Null-unsafe `GetHashCode` generation for types with >8 members
- Redundant allocation in `GetDiagnostics`
- `PascalToKebab` producing `x-m-l-path` instead of `xml-path` for acronyms
- `IsRequired` not detecting required value-type properties
- `IsHelpFlag` case sensitivity inconsistent with other CLI flag parsing
- `IsEnvironmentError` using fragile message-based detection instead of exception types

### Changed
- 557 total tests (269 Core + 206 Server + 82 CLI)

## [0.2.1] - 2026-02-06

### Added
- `sort_usings` tool -- sort using directives alphabetically in a C# file (19th tool)
- `allFiles` parameter for `add_missing_usings` and `remove_unused_usings` -- process every C# file in the solution with a single call

### Changed
- Server now reports its actual assembly version instead of a hardcoded value
- README updated to document all 19 tools

## [0.2.0] - 2026-02-06

### Added
- 10 new refactoring operations (18 total): extract variable, extract constant, extract interface, extract base class, inline variable, change signature, encapsulate field, generate overrides, implement interface, convert to async
- File-based logging for troubleshooting (`%TEMP%/roslyn-mcp/` or `/tmp/roslyn-mcp/`)
- JSON-RPC error responses and structured logging
- Unit tests for StdioTransport
- Pinned .NET SDK version via `global.json`

### Fixed
- Blocking async calls that could cause deadlocks in MoveTypeToFile and MoveTypeToNamespace
- MSBuildWorkspaceProvider reliability with better error handling
- CI workflow branch triggers (now correctly target `master`)
- NuGet push wildcard handling on Windows in CI

### Removed
- Broken integration tests with MSBuild assembly conflicts

## [0.1.0] - 2026-01-30

### Added
- Initial public release
- 8 Roslyn-powered C# refactoring operations
- Cross-platform .NET global tool (`roslyn-mcp`)
- MCP protocol support for Claude Code and Claude Desktop

[0.4.0]: https://github.com/JoshuaRamirez/RoslynMcpServer/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/JoshuaRamirez/RoslynMcpServer/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/JoshuaRamirez/RoslynMcpServer/compare/v0.2.1...v0.3.0
[0.2.1]: https://github.com/JoshuaRamirez/RoslynMcpServer/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/JoshuaRamirez/RoslynMcpServer/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/JoshuaRamirez/RoslynMcpServer/releases/tag/v0.1.0
